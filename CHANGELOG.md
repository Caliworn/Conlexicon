# Changelog

本文件记录 Conlexicon 的重要功能、行为与兼容性变更。

## New

### 改进

- 删除已经失去主体消费者的单边光学模式：Product 几何、缓存、正式表面注册和 Lab 不再提供“仅右边/仅下边”，所有液态玻璃表面统一按完整外轮廓生成四边折射与法线贴图。
- 清理 Liquid Glass Lab 重复的“引擎角色”字段：continuous/focus/floating/modal 仅作为参数预设，Product 实验表面统一注册为独立 `diagnostic` 角色；Custom 状态恢复默认值时回到最近一次预设，不再让角色和展开后的参数产生两套互相覆盖的控制入口。
- Liquid Glass Lab 的默认背景参照改为室内照片，打开实验页即可直接观察真实明暗边界和细节经过玻璃表面的折射。
- Liquid Glass Lab 将宽度、高度和圆角提升为三条渲染路径共用的表面几何参数；切换 Product Engine、Reference Baseline 与 SDF Baseline，以及恢复任一模型默认值时均保持当前几何，SDF 圆角比较快捷方式同步操作共享圆角。
- Liquid Glass Lab 的 SDF Controlled Comparisons 新增“Ref 趋近 · 光学”和“Ref 趋近 · +外观”临时快捷方式：两者共享同一组趋近 Reference 的高质量、宽边带、强位移 SDF 参数，并仅为对齐单路 Reference 关闭 RGB 色散；后者只额外复用当前 Reference tint 与内外阴影，便于直接分辨算法和外观包装各自造成的视觉差距。
- Liquid Glass Lab 新增共享外轮廓模型选择：Product 与 SDF 可比较“直边＋超椭圆角”和由整张表面 Lamé 方程定义的全局超椭圆，Reference 继续保持来源传统圆角；角部模型在指数 `2` 时复用 Round 快速路径，全局模型在指数 `2` 时为椭圆。圆角半径滑杆同时删除 `120px` 人为封顶，改为随共享宽高收束到短边一半；Product/SDF 全局模型不消费 radius，但会保留其值供切回角部模型。

### 修复

- Liquid Glass Lab 的诊断贴图预览不再被强制拉伸为固定 `2.2:1` 或使用像素化缩放；Product/Reference 贴图现在按实际表面比例显示，SDF 方形贴图保持方形，极端纵横比只限制最大展示尺寸而不改变比例。
- 重写 Product Engine 的宽边带方向场：精确 rounded-rect/superellipse 外轮廓继续决定边界和 rim，位移图与 normal/rim 图改为共享从真实边界法线平滑过渡到四边全支撑有理势场的连续方向，光学带外写入中性法线，不再由硬截断 edge influence 形成斜向分区；贴图生成同时复用 256 点折射 LUT，避免逐像素重复执行 Snell 幂运算。

## 2026-08-21

### 改进

- Liquid Glass Lab 新增三条路径共享保留的外轮廓选择：Product 与 SDF 可在传统圆角和指数 `2–8` 的 superellipse 之间比较，并让 CSS 裁切与贴图几何保持一致；本机 Reference Baseline 继续使用来源传统圆角并明确提示不支持。该实验不接入主体皮肤，Reference 与 SDF 也不再作为主体光学后端。

## 2026-08-20

### 改进

- 将液态玻璃导航从贴边整高侧栏重构为一张悬浮于全宽应用画布上的内嵌连续光学卡片：桌面展开与 rail、移动顶栏和抽屉均保留视口间距、完整圆角与四边几何折射，浅色导航改用与当前主题一致的明亮 regular glass；目的地保持平面行，active 保持非光学胶囊，底部命令改为分隔线分组而非嵌套卡片。尺寸变化时会立即释放旧几何并暂回普通玻璃，稳定后再生成精确贴图，避免宽度动效拉伸过期边缘；经典与层叠玻璃不受影响。
- 将无再分发许可的 Liquid Glass 逐行 Reference Baseline 从全部可达 Git 历史中移除，并以精确 `.gitignore` 路径保留为开发者本机研究材料。此前一并清理的四张参考背景已通过 Google Lens 找到独立原始来源，确认三张适用 Unsplash License、一张适用 Pexels License；图片按内容语义改名并从当前版本重新纳入跟踪，逐张作者、原始页面和许可记录同步补齐。
- Liquid Glass Lab 的场景选择器新增五张本地背景图：室内、Diamond Valley Lake 花丛、Whangarei Falls 栈桥、浅水岩石和草地人物。图片按选择加载，可在相同拖动位置和参数下观察大块明暗面、细密纹理、强色彩边界及人物轮廓的折射；素材保持 Lab 研究专用，逐张记录 Unsplash/Pexels 来源与许可，不进入产品皮肤。

## 2026-08-18

### 改进

- 收束皮肤选择器预览的文件职责：共享组件样式只保留菜单、选项和缩略图框架，经典窗口、层叠玻璃叠片和液态玻璃色场分别迁入对应皮肤文件；三套目标规则保持无当前皮肤作用域，因此任意皮肤下仍会同时显示全部候选预览，计算样式和交互不变。样式契约同步验证共享框架与目标外观的所有权边界。
- 完成皮肤样式基础拆分：`theme-classic.css` 独立承载经典明暗材质，经典、层叠玻璃和液态玻璃分别完整拥有全部标准颜色、材质与圆角 token，液态玻璃不再暗中继承经典 tooltip、toast 或词汇网络值。失去职责的 `theme-tokens.css` 及其根级启动浅色默认已删除；五种角色圆角由每套皮肤独立定义，当前数值保持不变，局部小圆角、液态导航专属层级和独立 Lab 也不受影响。四个没有消费者的历史 token 同步删除，样式契约改为验证完整皮肤所有权和角色圆角消费者，不限制各皮肤以后采用不同值。

## 2026-08-17

### 改进

- 收束液态玻璃工具导航的内部层级：桌面展开栏、rail 与移动抽屉继续共用唯一 continuous Q3 表面，普通工具行改为透明平面列表，hover 使用低透明状态底，active 使用无独立折射、无固定渐变和无外阴影的单色胶囊；底部皮肤、主题和语言命令合并为一个静态组合控件，导航行 pressed 不再位移。改动不增加滤镜、表面注册、监听器或动画帧，减少动态、减少透明度和强制颜色路径同步保留。
- Liquid Glass Lab 的 SDF 面板新增受控比较快捷方式：可一键恢复来源默认、隔离纯折射，并比较 Q128/Q1024、`depth: 30`、线性/球顶梯度和 `radius: 20/100`；每个实验预设都从相同默认值重建，纯折射系列统一关闭 glow、edge highlight 与 specular，避免要求人工逐项复原参数或把残留状态误当算法差异。

### 修复

- 清理玻璃按钮阶段泄漏到通用组件的 pressed 几何：主按钮、次按钮和危险按钮不再统一向下位移，只消费各皮肤的 pressed 阴影；导航目的地、皮肤、主题、语言和收起按钮改由皮肤 token 决定几何，经典与层叠玻璃在自身边界内缩放至 `0.985`，液态玻璃保持无缩放和位移。层叠玻璃同时修正高特异性 hover 阴影覆盖 pressed 阴影的问题，按住底部按钮不再扩大滚动范围且仍保留明确压力反馈。

## 2026-08-14

### 改进

- Liquid Glass Lab 新增隔离的 `SDF Baseline`：改编 MIT 许可 `PallavAg/liquid-glass-web-react` 的四象限圆角矩形 SDF、edge falloff、线性/球顶位移梯度、RGB 三路色散和 B 通道镜面合成，并保留来源的二值 Alpha 轮廓与方形贴图质量以供真实对照。三条路径共享背景、尺寸、圆角、拖动和读数；新基线不接入生产 Worker、角色 registry 或 LRU。新增第三方许可文本和研究记录，明确 Apple 仅公开设计/API、最初 Reference 来源未声明许可证，以及未经人工 Lab 验收的结果不得进入 Product Engine。
- Liquid Glass Lab 新增隔离的 `Reference Baseline` 渲染路径：以独立模块逐行移植 `archisvaze/liquid-glass` 当前 SVG 示例的 convex-squircle/Snell profile、全尺寸单路位移图、specular 图和原始 filter primitive 顺序，并保留其源默认参数；Lab 初始展示宽度独立设为可容纳示例文字的 `420px`。Lab 可即时切换 Baseline 与现有 Product Engine，贴图预览和状态读数随路径切换；参考实现不调用或改写生产几何、Worker、RGB 重组、角色注册与 LRU。
- 收束玻璃效果的样式与几何契约：删除 Lab 控件结构、精确 blur/bezel/tint/动画数值、旧滤镜与缓存标识符墓碑，以及将小圆角与宽 bezel、全宽 rim 误当长期不变量的断言；保留并改写为模块 API、主题分层、正式表面覆盖、辅助降级、单监听器、确定性贴图和字节预算回收等当前行为边界，避免后续光学算法修正被历史实现细节阻挡。
- 新增独立静态 `liquid-glass-lab.html`：直接复用生产几何、Worker、动态 SVG filter 和会话 LRU，可调角色、有效边、尺寸、圆角、bezel、厚度、IOR、位移、光学模糊、饱和度、tint、specular 与统一光源；提供连续色场/网格/色带/文字背景、Q3/Q1 与 effective bezel 状态、缓存统计及位移/法线贴图预览。预览卡片使用中性示例文字并支持舞台内合帧拖动，不再借用词典词条或展示 IPA/词性标签；实验页不接入产品导航、应用 API、词典数据或参数持久化，窄屏几何按舞台实际净宽收束。

### 性能

- 删除经典皮肤移动顶部栏不可见的 `12px` backdrop blur：该栏使用完全不透明的实色背景，模糊结果不会参与最终画面，只可能增加无意义的 backdrop 合成；层叠玻璃和液态玻璃继续使用各自显式定义的移动栏滤镜。

### 修复

- 修复 Liquid Glass Lab 浅色主题的通用 surface 阴影覆盖 Reference Baseline 来源外阴影的问题；Reference 现在在明暗场景下都使用来源项目的 `0 4px var(--reference-outer-shadow-blur) rgba(0,0,0,.18)`，不再混入 Product/Lab 的大面积投影。
- 移除 Liquid Glass Lab 的 SDF Baseline 上无来源的 tint、`1px` inset 边界与外阴影，使该路径默认只展示来源光学贴图经 Lab `backdrop-filter` 适配后的结果；Reference 和 Product 外观不变，减少透明度与强制颜色降级继续关闭光学伪元素。
- 修正三处共享辅助交互缺口：皮肤选择器键盘焦点环改用合法的焦点颜色 token；减少动态模式进一步关闭形态表、词根折叠箭头、质量问题提示和 toast 的空间位移 transition；Liquid Glass Lab 在减少透明度或强制颜色启用时不再只做 CSS 遮蔽，而会停止两条 Baseline 的 SVG/贴图及三条路径的诊断贴图生成，并让 Product Engine 实时切换至 Q0 实色降级。
- 修复 Liquid Glass Lab 在系统关闭透明效果时的暗色降级：Lab 的原生控件色彩模式现在与自身明暗主题同步；减少透明度使用主题化实色面板和文字，并完整关闭 Reference 的透明底色、滤镜及 tint/阴影伪元素，`Canvas` / `CanvasText` 仅保留给强制颜色模式，避免黑色玻璃与黑色文字叠加。
- 修正液态玻璃边缘的折射与镜面轮廓：位移沿负法线从玻璃内侧取样，外向法线继续只用于环境 specular；折射轮廓改为与参考项目一致的 128 采样 convex-squircle 截面和 Snell 路径，并在完整 effective bezel 内连续衰减，不再由快速归一化把有效折射压缩到最外数像素。specular 同样在完整 effective bezel 内连续衰减，不再保留未经验证的外侧 `18%` 人工限宽。几何缓存只存在于页面内存，新几何直接由有效边模型、effective bezel、尺寸和材质参数区分，不再维护无意义的版本字段。
- 修复液态玻璃四角可见斜线并恢复应用原有圆角：删除未提交批次新增的液态专用 `34/34/28/42px` 圆角覆盖，导航继续贴合应用边界，内容与浮层重新消费共享组件圆角；Lab 仍可独立调整圆角。几何层不再用最小圆角钳制整个表面的 bezel，continuous/focus/floating/modal 保留 `44/46/36/52px` 请求宽度；外轮廓使用真实组件圆角，宽边带重叠区平滑混合法线，避免 medial-axis 斜线。桌面导航与移动抽屉只生成右侧光学边，移动应用栏只生成下侧光学边；尺寸不足 `4px` 安全宽度的小表面稳定退回 Q1。缓存键和契约继续纳入有效边模型与 effective bezel。

## 2026-08-11

### 改进

- 校正液态玻璃 Q3 的正式光学主线：资源就绪表面现在只消费按实际几何生成的动态 URL filter，不再叠加角色级 `13–30px` 普通 blur；导航、抽屉、移动栏、当前词条、浮层、结构化 tooltip、modal 与词汇网络改用独立低透明中性 tint，并移除组件假渐变、固定方向彩色/明暗边、CSS radial/conic 假光、默认高光和 settle。当前方向性表现只来自 SVG 内的几何折射、RGB 色散与静态环境 specular；局部指针光照暂停至坐标模型另行设计。产品背景新增两层极弱固定交叉纤维参照，Q1 普通 blur、Q0 实色降级、micro 控件与 Q2 定义保持不变。
- 完成液态玻璃 LQ-6 旧光学路径收束：动态 filter 以几何位移图分别采样并重组 R/G/B，法线/rim 图与跨表面统一光源向量实时合成 specular，指针移动只更新现有矩阵而不重建贴图；正式表面增加单层局部焦散、方向性白色 glint 与资源提交 settle。删除 LQ-2 soft/strong 固定滤镜、`feTurbulence`、静态 fallback、青紫 inset 色边，以及 LQ-3 五层焦散和旧 pointer 状态；等待、生成失败或无 URL filter 时直接保留普通 blur，辅助模式继续使用既有实色降级，不改变公共偏好或业务接口。
- 完成液态玻璃 LQ-5 正式表面迁移：以引擎内唯一角色清单接入 continuous 导航、focus 当前词条、floating 菜单/来源建议/右键菜单/结构化 tooltip 与按需质量 tooltip，以及 modal/词汇网络；Q3 优先使用按实际尺寸生成的几何滤镜，LQ-2 固定滤镜只承担等待和 Q1 后备。临时与虚拟化表面显式注册/注销，micro 控件保持无逐项 backdrop 贴图的 CSS 体积材质，换皮肤与辅助模式会完整清理或关闭光学状态。

### 修复

- 补齐层叠玻璃主编辑外壳：互斥显示的 `.entry-display` 与 `#entryForm` 现在共同构成唯一当前词条工作区，桌面复用 `18px`、窄屏复用 `12px` 的 entry-detail 材质与固定双层低对比反射；表单内部字段仍不逐项模糊，查看/编辑外壳和高对比实色 toast 均不进入 LG-4C 指针点光 allowlist。
- 补齐液态玻璃正式表面覆盖：当前主编辑表单 `#entryForm` 与当前词条共用 `focus` 光学角色，表单内部字段仍不逐项折射；toast 在可见周期内显式注册为 `floating`，隐藏、换皮肤或辅助媒体变化时同步注销或重建角色与活动引用，预算内几何缓存可复用，并使用主题感知文字与 Q1/Q0 材质降级。
- 将明暗主题入口改为标准设置型 toggle：按钮标签固定为“暗黑模式 / Dark Mode”，并以 `aria-pressed` 表达暗色启用状态；浅色太阳与暗色月亮通过跨皮肤共享的 180ms 轨道动效自然交替，启动阶段和减少动态模式直接切换，避免目标图标动效产生昼夜语义倒置。
- 修复结构化 rich tooltip 在浅色主题下错误复用深底普通 tooltip 的浅色次级文字，并清理筛选面板未定义的颜色变量；经典、层叠玻璃和液态玻璃现在共享独立的 rich tooltip 主次文字契约，词性 chip 与中等级质量徽标也改用已有的高对比 warning 文本 token。液态玻璃 Q3 同时在动态 SVG filter 内按 continuous/focus/floating/modal 使用 `3/4/5.5/7px` 光学模糊并提高内容表面中性 tint，避免下层正文与面板内容重叠；产品背景撤销无实际光学意义的交叉纤维，仅保留连续低饱和色场和独立诊断网格。Q1、Q0、Q2、micro 控件和动态光源边界不变。

## 2026-08-10

### 改进

- 完成液态玻璃 LQ-4 几何与滤镜引擎：新增支持四角圆角、bezel、厚度和 IOR 的确定性折射/高光模型，专用 Worker 优先以 OffscreenCanvas 生成 PNG 贴图，并以可取消的分块主线程 renderer 后备；内部引擎提供动态 SVG filter registry、引用计数字节 LRU、ResizeObserver 尺寸合并、generation token、Worker 懒加载和换皮肤完整清理。新增只由 `?liquid-glass-diagnostics=1` 开启的独立验收表面，Chromium 明暗主题均确认 Q3 几何折射生效，现有正式消费者仍保持 LQ-1–LQ-3，等待 LQ-5 迁移。
- 冻结液态玻璃 LQ-4–LQ-7 全量光学重构设计：正常质量路径将从固定噪声位移升级为按表面尺寸、圆角、bezel、厚度和 IOR 生成的几何位移图与高光图，并通过独立 Worker、动态 SVG filter registry、字节预算 LRU 和角色化 surface registry 接入导航、当前词条、浮层、modal 与微型控件；同时明确参考项目的独立实现边界、结构化产品背景、RGB 边缘色散、光学分层、动态光照、辅助降级、共享模块影响和 LQ-4–LQ-7 分批验收。现有 LQ-1–LQ-3 保留为实施前基线及未来 Q1 降级，不代表最终视觉上限。
- 补齐两套玻璃皮肤的同类表面覆盖：液态玻璃的 chip list、标签信息和 rich 三种结构化 app tooltip 统一使用 `strong` 静态 SVG 折射与青紫色散边缘，并在减少透明度和强制高对比模式下完整关闭；层叠玻璃将来源建议和右键菜单纳入既有 LG-4C 指针点光 allowlist，继续复用单一监听器、逐帧合并、150ms 淡出及触摸/辅助模式停用边界。
- 完成液态玻璃 LQ-3 与整体验收：搜索/筛选、来源建议、右键菜单、皮肤选择器、modal 和大型网络面板使用单一文档级指针调度器与 `requestAnimationFrame` 合帧的中心焦散/近边缘 glint；离开、失焦、resize、页面隐藏、换皮肤以及触摸、减少动态、减少透明度、强制高对比会关闭或清理效果。动态白名单不包含当前词条、移动顶栏、tooltip、列表/分析卡片、表格行或网络节点；只读 20k 视图在桌面/移动端及滚动后保持有界渲染且卡片无 blur/指针属性。
- 完成液态玻璃 LQ-2：新增页面级静态 `soft` / `strong` SVG 折射滤镜，以固定低频位移分别服务当前词条、移动顶栏、大型网络面板和当前打开的小型浮层/modal，并通过约 1px 内侧青紫边表达克制色散；实现不生成 Canvas、不监听尺寸/滚动/指针，也不触及列表等重复表面。URL backdrop filter 不可用时保留普通 blur，减少透明度和强制高对比时关闭折射与色散。
- 完成液态玻璃 LQ-1：新增独立 `liquid-glass` 皮肤 ID、双语名称、选择器预览、首屏预着色和全局偏好持久化；`theme-liquid-glass.css` 提供明暗 token-only 材质、连续环境色场、分层透明/模糊/阴影以及 blur 不可用、减少透明度和强制高对比降级。当前阶段不包含 SVG 位移折射或动态焦散，相关光学增强留待 LQ-2/LQ-3。
- 完成质量检查 F5-0 契约冻结：为现有 12 类规则增加稳定 issue code、严重度/模块、结构化参数和 issue/entry/global 摘要，计算结果不再受界面语言影响；简单结构规则复用查询能力，复杂规则保留 Gloss 扫描、聚合和来源拓扑适配器。新增完整规则定向检查与独立质量结果计划，当前前端继续兼容本地化 `title` / `detail`，F5-1 再接入 QualityService/API。
- 将二态循环皮肤按钮升级为可扩展的悬浮皮肤选择器：侧栏入口统一使用衣服图标并显示当前皮肤，桌面展开栏、窄轨和移动抽屉共用不会被导航裁切且层级高于抽屉的上拉菜单；菜单支持皮肤预览、当前项标记、键盘导航、失败回滚和未来注册更多皮肤，并在层叠玻璃下复用 LG-4C 指针点光。

### 修复

- 修复经典皮肤暗色模式下覆盖面板材质别名在根作用域提前解析为浅色 floating 值、导致词汇网络和普通 modal 背景变白的问题；暗色作用域现在重新绑定同作用域 floating 材质，样式契约同时扫描所有依赖暗色覆盖项的纯 token alias，防止同类遗漏复发。

## 2026-08-09

### 改进

- 完成层叠玻璃皮肤阶段收尾：LG-1–LG-4C.1 全部转为完成状态，固定双层反射最终限定于单一词条详情外壳，局部指针响应限定于搜索/筛选浮层、信息与确认弹窗和词汇网络面板，桌面与移动导航统一使用无 blur 实色底面；主背景环境视差维持回滚，LG-4D 滚动适应不再列入计划。
- 更新皮肤切换按钮图标：经典皮肤使用平面窗口图标，层叠玻璃使用三层叠片图标；图标与按钮当前显示的目标皮肤同步切换。
- 让层叠玻璃的桌面与移动导航统一恢复经典皮肤的稳定实色底面：浅色使用 `#22313a`，暗色使用 `#10181c`；移动抽屉同步关闭已经没有视觉意义的 backdrop blur，继续保留覆盖层、边缘、阴影和导航控件状态高光。
- 将 LG-4C 指针点光收束为弹出表面效果：左侧导航栏退出目标白名单并删除导航专用点光参数，导航继续保留稳定底面及按钮自身的静止、悬浮、按下和选中高光；搜索/筛选浮层、信息与确认弹窗和词汇网络面板继续响应精细指针。
- 删除未被任何界面创建或打开的原生 `<dialog>` 死代码：移除经典/通用组件样式、原生 backdrop 规则、层叠玻璃指针响应选择器及 JavaScript 目标白名单；现有基于 `role="dialog"` 的搜索、筛选、词汇网络、信息和确认弹窗保持不变。
- 收束层叠玻璃的固定反射：对全部玻璃组件移除双层线性反射并完成对照后，只为单一词条详情外壳恢复双层反射；浮层、移动栏、导航、抽屉和大型覆盖面板保持无固定渐变，继续依靠半透明底色、背景模糊、边缘/厚度阴影及 LG-4C.1 局部响应表达材质。
- 收束层叠玻璃的后续边界：LG-4D 滚动适应不再列入计划，并移除没有实际组件消费者的 sticky 材质 token、降级定义与契约检查；所有现有 `position: sticky` 布局行为以及 navigation、mobile-bar 和浏览器面板材质保持不变。主背景环境视差试验维持回滚状态。
- 将现有玻璃皮肤统一命名为“层叠玻璃 / Layered Glass”：运行期 ID 改为 `layered-glass`，同步重命名 CSS 作用域、指针效果标识、样式文件、设计规范、偏好 API 契约与测试；旧 ID 不作为别名保留，未知旧偏好按既有规则回退经典皮肤，从而为未来独立的液态玻璃实现释放名称空间。
- 重新平衡层叠玻璃的模糊可见度：结构化 rich tooltip 底色从浅色 82%/暗色 84% 收到 68%/70%；modal 与词汇网络面板拆出浅色 38%/暗色 44% 的独立 overlay-panel 材质，不再共享普通 floating 底色，大型面板同步收弱固定反射以避免 washed-out；modal 遮罩降至浅色 32%/暗色 38%，网络强遮罩调整为 38%/44%；浅色移动抽屉从 89% 收到 76%。各角色保留原 blur 半径、静态反射和降级边界，词条详情、列表卡片及普通小浮层保持不变。
- 完成 S0 样式解耦：新增独立 `theme-tokens.css`，把明暗主题拆为语义颜色、panel/control/inset/floating/sticky/navigation/tooltip/overlay 等材质角色和少量几何 token；组件样式已全部改为消费角色 token，并删除旧变量、迁移别名、组件级暗色分支和主题材质字面量。新增样式契约检查持续验证加载顺序、token 完整性和文件责任边界；浏览器确认拆分前后关键计算样式一致，并完成中英文、明暗主题、320/480/768/1024/1440px、筛选浮层、移动抽屉和确认弹窗验收。
- 冻结层叠玻璃皮肤视觉规范：以稳定内容层和玻璃功能层分离为核心，明确各材质角色的首版透明度、模糊、边缘、阴影和几何方向，以及禁止逐卡片模糊、实色降级、可访问性、性能预算和完整验收要求；后续按 token-only 原型、运行期接入和可选动态增强三个独立阶段实施。
- 完成层叠玻璃 LG-1 token-only 原型：新增只在 `data-ui-skin="layered-glass"` 下生效的独立明暗 token 覆盖文件，内容卡片保持近不透明，导航、sticky、移动栏和临时浮层使用有界背景模糊；同时提供 blur 不可用、减少透明度和强制高对比降级。经典皮肤仍为默认，不新增设置、持久化字段、组件 DOM 或动态光学层；样式契约检查同步验证加载顺序、作用域和基础 token 完整性。
- 完成层叠玻璃 LG-2 运行期接入：新增独立于明暗主题的全局 `uiSkin` 偏好及经典/层叠玻璃切换按钮，启动预着色和服务端状态统一识别 `classic` / `layered-glass`；切换只更新外观和按钮文案，不重绘业务页面或丢失未保存编辑。未知读取值回退经典皮肤，非法写入返回结构化错误，SQLite schema 与词典设置保持不变。
- 提高层叠玻璃皮肤的日常视觉辨识度：画布增加低对比连续环境色场，使导航、sticky 与浮层的透明和背景模糊在静态页面也可感知；同时加强导航控件、面板、输入控件和浮层的边缘高光与厚度阴影，并将浅色模式侧栏收束为低透光的深色玻璃，避免亮色画布把工具导航抬成刺眼灰面。词条卡片、分析卡片和表格仍不逐项启用 blur，减少透明度、不支持 blur 和强制高对比时继续回退为实色。
- 完成层叠玻璃 LG-3 验收：覆盖中英文、明暗主题、320/480/768/1024/1440px、主要页面与浮层，并以临时 20k 词典确认虚拟列表渲染量和常驻 blur 层数保持有界；补强强制高对比下导航、面板和控件的系统色降级，样式契约新增降级、重复表面禁用 blur 及禁止动画化 blur/大阴影检查。动态折射、形状融合和设备方向响应经复评不纳入首版，指针追光仅保留为完成手工逐帧性能分析后的限定实验项。
- 整理按钮阴影层级：层叠玻璃皮肤为普通控件、绿色/红色实体动作及连续导航内控件提供有区别的静态高度、hover 和 pressed 反馈，焦点改用不覆盖材质阴影的独立轮廓；ghost、菜单、标签页、禁用状态和经典皮肤继续保持无外投影，危险动作使用中性暗影而非彩色发光。
- 完成层叠玻璃 LG-4A/B 强化：降低 panel、list-item、browser、inset 和 control 的不透明度但继续禁止内容层 blur；navigation、mobile-bar、sticky 和 floating 进一步透出环境，并以共享材质 token 叠加两层不对称静态线性反射。搜索/筛选菜单、来源建议、右键菜单、dialog、modal 和词汇网络面板统一获得光学层，不增加组件 DOM；实色降级与经典皮肤保持不变。
- 统一层叠玻璃控件的镜面高光状态：内容控件、实色动作和导航控件分别使用明确的静止、悬浮与按下阴影令牌，浅色与暗色高光均提高可见度并以微暗下缘建立厚度；侧栏主导航、收起按钮和底部皮肤/主题/语言按钮不再混入通用次级按钮阴影，强制高对比模式继续关闭装饰性高光。
- 提高层叠玻璃浅色模式导航的对比度：加深连续玻璃底面并减弱整面白色反射，拉开普通、悬浮、当前页面和底部全局动作的底色与边框层级，同时提亮品牌辅助文字；暗色模式和经典皮肤保持原有配色。
- 将层叠玻璃 LG-4C.1 调整为收窄的局部点光与边缘响应试验：连续导航及当前搜索/筛选浮层、对话框和 modal 在固定基础材质上叠加宽而弱的光标点光，并仅在距边缘 `72px` 内显示局部边缘高光；大型词汇网络面板的点光收窄到约 `400px`，浅色响应提高到约 `11%` 点光与 `8%` 窄幅边缘高光，暗色模式保持较弱响应，既有 `28px` 模糊不变。指针离开淡出、单一全局监听、逐帧合并及触摸/降级模式停用边界保持不变。
- 按布局语义拆分层叠玻璃导航材质：桌面展开栏和窄轨改用不透明深色底面并关闭无意义的大面积 backdrop blur，移动覆盖抽屉通过独立 `navigation-drawer` 角色继续保留有限透明和模糊；静态双层反射、LG-4C 指针响应、按钮高光与导航 DOM 保持共享。
- 试行词条详情聚焦玻璃：查看态外壳使用专用的静态双层反射、有限透明和背景模糊，词形、IPA、词性、标签与操作直接位于同一表面；释义、词源、衍生词、形态和备注继续使用近实色且无模糊的内部区块。窄屏提高底色不透明度并降低模糊，经典皮肤仍使用实色材质，LG-4C 指针响应及其他内容卡片保持不变。
- 统一词条筛选 UI 与运行期状态：原词性下拉移入列表控制栏的“筛选”面板，筛选图标补充动态 tooltip；词性改为标准 `EntryFilter.part` 并与普通结构条件、feature result 和旧本地质量结果共用唯一 `activeFilter`、当前筛选状态栏及循环/重试/清除控件；删除独立 `activePart`、专用词性动作和进入筛选前的视图快照。清除或替换筛选保留当前搜索和排序，从分析页或词条标签进入词性条件也复用普通查询窗口；旧本地质量结果在 F5 接线前临时按当前搜索配置与既有 ID 集合求交。
- 新增词典级活跃标签身份快照：`/facets` 以唯一词条为单位一次聚合当前原始标签、词性身份、显示替换碰撞与无词性计数，并按词典 generation 缓存和失效；词性/其他标签/标签集合分析、词条列表与详情、词性菜单和高级筛选标题统一消费服务端 `displayAmbiguous`，不再因 widget 截断或页面局部作用域产生不同的消歧结果。
- 完成 F4 UI 验收：在中英文、明暗主题和 320/480/768/1024/1440px 下检查总览及全部分析子页、长标签布局、标签集合筛选与词根家族跳转，未发现横向溢出、卡片重叠、滞留加载状态或控制台错误；同步补齐标签集合、正写法、完整活动日期、词根家族和形态 source/view 的计划记录，F4 阶段正式收尾。
- 总览词性分布移除后端 widget 截断和前端 Top N，展示全部实际词性；总览与词汇标签详情页统一在排行顶部显示词性种类、底部独立显示无词性词条数，无词性统计继续提供筛选入口。
- 标签分析统一显示身份消歧：共享模型按明确作用域建立“显示文本 → 原始标签集合”索引，仅在多个原始标签使用同一显示替换时补充原始值；标签集合、词性分布、其他标签分布及总览词性卡片复用同一规则，筛选身份、统计顺序和后端契约保持不变。
- 完成正写法分析迁移：新增共享 `orthography-model`、轻量 `orthographyDistribution` widget 和普通 `orthography` 结构筛选，词长、首字符、字符与双字符统计不再扫描前端完整词典或携带固定 ID；双字符改为只统计 Unicode 空白分隔片段内部的相邻码点，不再跨空白拼接。正写法页接入异步加载、失败与重试状态，并删除最后的旧 analysis slice 及 `analysis-model`。
- 收窄 `coverageBreakdown` 契约，只返回总览实际表达的释义、例句、IPA 与备注覆盖；移除与 `lexiconSummary` 词根/衍生词构成重复且无消费者的来源覆盖行，底层来源计数继续由同一 `entryStats` 任务共享。
- 收紧新建词条 ID 契约：`POST /entries` 现在拒绝客户端携带 `entry.id`，由后端只生成一次 UUID；create-only 与全局实体冲突检查继续保证理论碰撞不会覆盖旧实体，但不为近乎不可能的 UUID 碰撞增加循环重试、专用生成器或额外恢复机制。
- 统一其余随机 ID 的最小防御边界：词条子实体、形态/语料实体和词典 ID 缺失时只生成一次 UUID，删除人为注入随机碰撞并期待重试的测试；新建词典不再通过覆盖式导入落盘，显式重复 ID 返回冲突，理论上的后端生成 ID 碰撞作为内部失败且不会覆盖现有词典。显式导入覆盖确认和完整实体唯一性检查保持不变。
- 形态共享模型不再用局部 `morph-1` / `mtable-1` 确定性编号为缺失 ID 的对象补值；纯规范化只保留已有 ID，正式前端创建和后端保存/导入边界继续通过共享 UUID 分配器补齐持久化 ID，避免多个无 ID 模板组在独立局部集合中得到相同编号。
- 词典卡片的词条数和词根数现在只消费后端 summary；词条保存和删除响应同步返回写入后的轻量计数，不再由前端完整词典或旧关系模型兜底重算，并删除已无路由消费的 `dictionary-query-model`、relation slice、对应测试与前端包装函数。词性筛选选项也只消费 facets API 的成功结果，移除请求未完成或失败时扫描完整词典重算词性集合的真实前端兜底。
- 编辑器导航不再扫描完整词典验证选中 ID、排序选择首个 lemma 或为词性/旧本地高级筛选挑选首项：初始选择复用普通查询首窗，显式过期目标恢复原选择，删除当前词条按已加载虚拟列表顺序选择邻项；删除未选中词条保持当前选择和未保存编辑表面。

### 新增

- 完成 F4b-2 IPA 分布迁移：新增独立 `ipaDistribution` 功能结果源及完整 summary/items/location 契约，按需统计音素频次与唯一词条数、首音、尾音和音节数；数据分析的分布与音位页改为异步共享同一份 summary，提供加载、失败与重试状态，点击统计桶后由 `category + value` 功能结果查询接入普通词条窗口、搜索、排序、定位和 cursor，不再保存前端固定 ID slice。分布计算不会调用自动 IPA 引擎，其缓存身份只纳入复杂音素与音节分隔规则。

### 改进

- 数据分析“词汇 > 标签”新增后端 `tagSetDistribution` widget：按至少含一个标签的词条聚合无序完整原始标签集合，单标签集合明确表示词条仅有该标签；无标签词条作为独立计数和可点击筛选显示，不作为空集合混入排行或集合种类。显示替换与显示顺序不改变集合身份，集合行使用 `exact` 标签结构筛选排除超集；有标签、无标签和多标签摘要分别使用 `presence.tag: true`、`presence.tag: false` 和 `tagCount.min: 2` 进入普通词条查询，高级筛选标题统一使用“有标签词条 / 无标签词条 / 多标签词条”的结果集合口径。完整排行首批渲染 100 行并可在不追加请求的情况下继续展开。旧前端标签组合切片及固定 ID 高级筛选已移除。
- 收紧数据分析柱状图标题语义：普通分布继续默认以词条为统计单位，不重复显示行级单位；“其他标签频率”改称“其他标签分布”，IPA 首尾音、形态覆写分组和新增/编辑日期标题明确其分布对象，中英文标题保持一致。出现次数、衍生词数和单元格数等非默认单位继续在标题中显式标注。
- 收束数据分析统计单位契约：facets、F4a widgets/filter actions 以及 IPA/形态 feature summary 使用 `entryCount`、`resultCount`、`occurrenceCount`、`coveredEntryCount`、`derivedEntryCount` 等显式字段，直接修订尚未发布的 v1 契约且不保留旧字段兼容层。IPA 音位与正写法字符/双字符频率同时展示出现次数和贡献词条数，词根家族与覆写排行明确标注其排名单位。
- 统一词条编辑、词性配置、标签排序/高亮、形态模板匹配和语料标签的中英文输入说明，补全可接受分隔符、形态表格组首个命中和标签显示替换边界；词性与排序输入框的示例占位符会随当前标签列表分隔符动态更新，但不改写尚未保存的输入。
- 标签列表统一使用共享的文本解析、模型规范化与显示序列化：词条、词性配置、标签排序/高亮、形态模板匹配和语料标签均稳定去除空项与精确重复项，并可按词典选择半角逗号加空格、全角逗号或顿号作为重新显示时的分隔符。红色高亮标签改为单行输入，不再把标签内部空格或换行当作特殊分隔语义。
- 词性识别收束为单一显式配置：只有“其他设置”中列出的标签会被识别为词性，列表为空表示词典不使用词性；词性配置移至词条标签卡片顶部，并删除首标签自动词性开关、位置判定、查询分支、缓存身份和新库专用部分索引。旧开关键不再参与行为，也不在运行时清理或转换。词条编辑字段统一称为“标签”，并以独立帮助文本说明词性与其他标签在同一字段中填写。
- 数据分析“词汇 > 标签”的完整词性分布与“其他标签分布”改为按需请求 SQLite analysis widgets；其他标签从聚合阶段明确排除显式词性标签，点击后分别生成结构化词性或标签筛选。旧前端切片只继续计算标签组合，不再重复扫描词性与单标签频率。
- 数据分析顶层“词条与标签 / Entries & Tags”更名为“词汇 / Lexicon”，其“词形结构 / Word Forms”子页更名为“正写法 / Orthography”，使标签、正写法和词根家族统一表达为词汇系统的分析维度；内部路由保持不变。
- 收紧 Windows 协作命令规则：当前 `shell_command` 统一显式进入 PATH 中的 Git Bash，不再使用 PowerShell/cmd 语法；禁止通过 detached 子进程伪造常驻开发会话，避免命令 runner 持续等待进程树。
- 数据分析子页“词根关系”更名为“词根家族”，并改为按需请求完整的 `rootFamilyRanking` widget，直接复用词根模式和词汇网络的稳定 `RootTopologyCache`；排行返回全部非空家族的词根 ID、词形和衍生词数，不再沿用仅适合总览小组件的 Top N 截断，也不再由前端完整词典重建关系索引或保留家族成员 ID。
- 删除 repository contract 查询矩阵中一条参数与断言完全相同的 definitions fuzzy 重复查询，不改变现有搜索覆盖范围。
- 完成测试脚本收尾：统一入口现在会校验所有 `check-*` 脚本均已登记且没有重复或失效项；移除重复的 SQLite schema/lifecycle 聚合脚本，将误称 migration 的旧 JSON 目录检查改为 conversion，并同步标准验证命令和测试夹具词性断言边界。
- 修订项目协作规则：区分只读与修改型 Git 操作、任务内旧实现清理与破坏性删除，按实际改动文件执行 JavaScript 语法检查，并明确当前 SQLite schema 不使用运行时迁移链；同时修正 Windows shell、Changelog 条目归属、SQLite 存储措辞和 UI 验收边界。
- 统一测试入口为 `node scripts/check-all.js`，按纯模型与 SQLite/API 两组顺序运行现有回归并为单脚本设置超时；SQLite 已是必需运行时，相关检查不再静默跳过。清理仅记录已删除 analysis slice 和历史 schema 名称的墓碑断言，同时在项目协作规则中限定负向断言必须对应当前公开契约或明确架构不变量。
- 数据分析“编辑进度”的完整新增日期与编辑日期改为按需异步请求 SQLite `activityDistribution` widget，不再读取完整活动词典并在前端扫描全部词条分桶；总览继续使用有界 `activityPreview`，两种 widget 在同一请求中共享一次活动统计任务，日期行仍直接进入结构化日期筛选。
- 完成 F4b-3 形态分析迁移：冻结并实装 `morphologyAnalysis` 的 summary、items、location 和高级筛选 category/value/scope，统计真实模板组分配、自动/手动模式及 nested override 的 active/inactive 覆写。数据分析形态页改为按需异步加载“使用情况/覆写”，统计项直接进入可搜索、排序、定位和循环的功能结果窗口；删除旧前端单表适配、固定 ID slice、生成数、空单元、子表使用排行和形态质量判断。Feature service/query model 使用显式 source registry，SQLite 只提供不含释义、来源、IPA、模板单元格或完整词典快照的最小形态输入。
- 将 1k、3k、10k 和形态专项压力词典生成器统一为一个可配置脚本：必须显式指定空或仅完成空索引初始化的测试数据目录，可通过 `--entries` 调整规模，始终生成形态模板与稀疏覆写并直接写入 SQLite；名称、描述和完成摘要统一反映实际词条数，同时禁止写入项目真实 `data/`。
- 将普通词条和父级词根组公共查询窗口硬上限分别收窄为产品实际使用的 200 条与 100 组，超限请求改为明确验证错误；移除供旧基准使用的 10000/2000 大页旁路。旧 JSON 输入的搜索基准已删除，其 strict/fuzzy、静态/形态字段场景合并进 SQLite 查询会话基准，统一报告冷构建、热窗口和缓存指标。
- 词条列表的运行期搜索字段面板新增逐字段命中词条数：普通查询、结构筛选、词根模式和 feature result 均按当前启用字段及各自 strict/fuzzy 配置返回一致的唯一词条计数；词根模式统计直接搜索命中的词条，而非可见词根组或随组展示的衍生词。移除数据分析中重复且依赖完整活动词典的“当前搜索命中字段/当前搜索命中”统计。
- 移除与总览“资料覆盖”重复的数据分析覆盖率子页、详情跳转和前端全词典 coverage slice；释义、例句、IPA 与备注覆盖仍由总览 SQLite widget 展示并可直接进入有/无字段筛选，Gloss 问题继续归入质量检查。

### 性能
- 正写法分布保持最小 `id + lemma` 扫描且不新增持久化 projection；临时 20k 纯内存基准中完整分布约 322 ms，优化后的首次 SQLite 双字符结构筛选约 110 ms，暂不需要持久化 projection 或后台任务。
- 形态模块完整 PUT 改为后端差异执行：模板组、子表和单元格按稳定 ID 增量写入，规则、函数和自动匹配变化只重建实际受影响词条的形态搜索 projection；纯名称/说明变化不再触碰生成投影，规范化后完全相同的保存不更新时间或缓存。删除、移动或缩小模板结构若会留下词条形态组或覆写悬空引用，现在会拒绝保存而非静默产生无效状态。临时 20k 形态词典中，纯显示修改约 8ms 且没有删除投影记录；单个规则单元格修改只重建实际匹配的 2813 个词条和 33756 条既有记录，约 990ms。
- 实体 ID 唯一性检查改为后端按本次提交范围执行：新词条 ID 由后端生成，普通词条、语料和形态保存先检查 payload 内部重复，再对 SQLite 主键表做分块定向查询并仅读取必要的 blob 范围；前端不再枚举完整词典预检 UUID、导入前重复扫描或发送创建存在性探针。完整导入仍保留全量唯一性校验，结构化冲突详情由应用内弹窗显示。
- IPA 分布构建每 128 项让出事件循环；临时 SQLite 基准中，10k/30k 词条冷构建约 50/130 毫秒，热桶查询约 2 毫秒，暂不需要引入后台任务状态。
- 字段命中计数与有序结果 ID 由同一轮搜索 projection 的去重 `(entryId, field)` 在 SQL 内聚合为一词条一行的字段位掩码，并随普通、词根或 feature 查询会话缓存；打开字段面板、翻窗和定位均不追加匹配查询，未启用字段不会进入 projection 扫描。临时 10k/30k 形态压力词典中，全字段 fuzzy 重复查询中位约 175/656 毫秒，静态字段 fuzzy 约 118/478 毫秒，未出现按字段数追加查询的放大。

### 修复

- 修复词条列表标签身份浮窗、隐藏标签列表浮窗和词汇网络节点详情浮窗只使用半透明或实色背景、未接入结构化背景模糊的问题；这些 rich tooltip 统一使用独立皮肤材质，层叠玻璃启用有界 `18px` blur 与轻微饱和，经典皮肤、减少透明度、不支持 blur 和强制高对比路径继续为 `none`。样式契约同时审计所有 active glass background consumer 必须配套对应滤镜，防止新增组件只接背景不接 blur。
- 修复词汇网络和普通 modal 的全屏遮罩通过 `opacity` 入场动画建立 Backdrop Root、导致内部面板虽声明 `backdrop-filter` 却只能采样均匀遮罩的问题；遮罩改为仅淡入背景色，网络面板只保留位移与缩放，局部玻璃现在能真实模糊下方页面，减少动态模式继续关闭相关动画。
- 修复词条详情专用材质的经典暗色默认值在根作用域提前解析为浅色、导致切换经典皮肤后详情外壳和栏目错误变白的问题；经典暗色现在显式绑定同作用域 panel 值，样式契约持续检查这一覆盖边界。
- 修复移动导航抽屉材质别名在根作用域提前解析为经典浅色导航值、导致经典暗色模式仍显示浅色底面的问题；暗色作用域现在重新绑定同作用域 navigation token，样式契约同步检查这条语义关系，后续修改导航颜色时无需重复维护抽屉值。
- 修复 checkbox/radio 被通用文本输入材质规则套用顶部玻璃高光、并在鼠标点击后持续显示焦点环的问题；原生选框不再使用 control shadow，键盘导航仍通过 `:focus-visible` 显示明确轮廓，经典与层叠玻璃皮肤行为一致。
- 修复 320px 英文移动导航中长工具名虽已省略、工具列表仍因双轴 `overflow: auto` 显示横向滚动条的问题；导航列表现在只允许纵向滚动。
- 修复层叠玻璃皮肤下词条列表控制栏与连续页面背景之间的细微接缝：该控制栏位于独立滚动列表之外，不再执行无意义的背景模糊或 sticky 定位；桌面端由内层列表窗口负责裁切，外层允许完整绘制控件阴影，移动抽屉继续保持边界裁切。实际固定位置和操作行为保持不变。
- 修复经典皮肤暗色模式遗漏黄色标签背景 token、导致词性及琥珀色标签继承浅色背景而对比不足的问题；黄色标签现在使用与暗色警告语义一致的深色底面。
- 词汇网络的直接来源与直接衍生计数移出可滚动画布，改为标题下方稳定的双列摘要栏；删除会与 SVG 节点或连线重叠的绝对定位层级标签。窄屏纵向网络新增参与真实布局的“来源 / 当前词条 / 衍生”分组标题，避免单列卡片模糊关系方向；节点卡片将 IPA 与词性拆为独立行，并在字段缺失时自动重新居中。
- 词汇网络 SVG 画布移除额外边框和底色，直接融入外层玻璃面板；关系图内容少于可用高度时由 SVG 在 viewport 内纵向居中，内容较多时继续保持原有尺寸和滚动行为。
- 修复新建词条草稿的空 `id` 属性仍被序列化进 `POST /entries`、触发后端 `entry_id_not_allowed` 的问题；创建请求现在在 API payload 边界无条件移除 ID，编辑 PUT 仍保留既有 ID。
- 修复数据分析“词汇 > 标签”新增组合卡片后，通用自动列网格会在宽屏生成空置第三列、导致词性与其他标签卡片意外变窄的问题；该页改用专用的等宽双列加全宽组合卡布局，并按分析内容区宽度切换为单列。
- 修复数据分析与质量检查中的词条链接只更新内部页面状态、未切换可见视图的问题；词根家族排行及其他直接词条入口现在统一通过正式导航进入编辑器后选中目标词条。
- 语言与主题切换不再复用会重新填充表单的完整渲染：完整/局部词条编辑、词典配置、其他设置、IPA、形态表格和语料编辑均原地更新动态文案，保留未保存值、项目顺序和编辑 DOM；词条筛选栏与虚拟列表使用不启动查询的 locale 展示路径。旧本地质量筛选只替换按稳定 key 对应的问题文案映射，不再改变当前变体、选中词条或编辑模式。
- IPA 分布 summary 现在同时返回词典总词条数与实际参与分布的 IPA 词条数；IPA 覆盖率及有/无 IPA 筛选不再读取前端完整词条数组作为分母。
- 数据分析总览根据分析内容区的实际可用宽度在两列与单列之间切换，不再依赖包含侧栏的视口宽度；资料覆盖列表改用共享列网格，使不同长度的数量说明不再挤压进度条并造成行间错位。
- 搜索模型的原始文本匹配与模糊评分 helper 不再在遗漏运行期 normalizer 时静默退回固定 `zh-CN` 小写规则；调用方现在必须显式注入搜索规范化函数，避免绕过词典搜索配置。

## 2026-07-27

### 改进

- 数据分析总览按最新设计收束为词汇构成、资料覆盖、词性分布和编辑活动四张卡片：首张卡片以双列统计和构成条明确展示互斥的词根/衍生词，并将多来源词条标为衍生词子集及其占比；总数合并显示为“109 个词条 / 109 entries”式的紧凑数值组，其余卡片使用带方向图标和明确边界的详情按钮。移除含义不清的词条总数跳转和错误指向词根家族排行的详情入口；卡内统计可直接筛选，其余卡片详情进入对应分析子页，首屏不重复展示来源覆盖，也不计算词根家族或孤立词根拓扑。
- 高级筛选语言切换不再失效查询窗口或结构 facts；筛选及循环变体改为保存语义化 `titleDescriptor`，统一由主 i18n 字典在渲染时生成标题，删除按旧中英文字符串反查语义的第二套映射。旧本地质量筛选只为问题文本定向重建；稳定结构筛选和 IPA feature 筛选正常时隐藏刷新按钮，远程查询失败时才显示为重试入口。

## 2026-07-24

### 改进

- 严格定义基础关系统计：没有来源记录的词条计为词根，至少有一条来源记录的词条计为衍生词；未解析来源产生的词根模式回退组不再污染 `rootCount`。`/api/state` 现在通过轻量 SQL 为所有词典返回词条数和词根数，词典管理卡片恢复显示词根数量，并且启动、切换和列表读取不再为摘要建立稳定词根拓扑。
- F4b feature query 新增显式 `summary` 响应模式，只按 result source 建立或复用基础会话，不接受 view/page，也不构建分类排序视图、EntrySummary、分页信息或 cursor；IPA 自动检查页首次加载改用该模式，移除 `limit: 1` 返回的无用首条词条。
- 数据分析移除与词条列表“编辑时间倒序”重复的“最近修改”列表及子页；编辑进度继续保留创建日期和编辑日期聚合，后端 `activityPreview` 不再执行最近词条排序查询。
- 实装 F4b-1 IPA 功能结果会话：新增可替换简易音系引擎 adapter、可重建 `ipaAutoCompare` result source、有界运行时会话缓存及 query/location API；自动生成比较改为互斥的精确一致、仅规范化一致、不一致、无法生成和生成失败 outcome，分析页三类统计与循环变体共用一次 summary，不再传递完整词条 ID 数组。
- IPA 自动生成检查页改为按需异步加载，提供加载、失败和重试状态；点击统计后，词条列表复用现有虚拟窗口、搜索字段/fuzzy 运行期配置、排序和远端定位消费功能结果，分类切换、搜索、排序和翻窗均不重复执行 IPA 生成。自动生成值继续只作为可重建运行期结果，不新增 SQLite 持久化列。
- 完成 F4b-0 功能结果会话清点与独立复评：冻结可重建 result source、分类/搜索/窗口视图、generation/设置/引擎失效和同步优先的缓存边界；确定 F4b-1 以可替换音系引擎适配层承接当前 IPA 自动生成比较，不提前持久化自动 IPA。词根家族排行改走稳定 topology summary，正写法统计改走轻量 summary/facet，质量检查留在独立 F5 `/quality/query`。
- 词条列表搜索框右侧新增运行期搜索配置按钮，可逐字段临时调整是否参与搜索及 fuzzy 状态、恢复词典默认值，并以强调状态提示当前配置已偏离默认；最后一个启用字段保持正常外观，尝试关闭时会恢复勾选并提示至少保留一个字段。“其他设置”中的原字段配置改为新会话默认值说明，移除多余的“模糊匹配”二级标题。数据分析“当前搜索命中字段”点击后直接切换该运行期配置，不再创建需要退出或循环的高级筛选；搜索分析缓存同时纳入运行期字段/fuzzy 签名，不再复用其他配置生成的旧命中字段统计。
- 高级筛选将结构候选状态与当前搜索命中拆分：循环按钮只读取按词典版本缓存的 filter facts，搜索输入只重查当前变体；进入筛选和成功写入后会自动补齐未知结构事实，有效的非当前响应也会按查询身份保留复用。
- 实装数据分析 F4a：新增同步、按需的 `/analysis/query` 轻量 widget API 与最小 planner，支持词条数、覆盖率、词性分布和活动预览；前端总览改为异步加载结构化 DTO，并提供加载、失败和重试状态。
- 项目级协作规则适配 FastCtx 开发环境：本地读取、搜索和文件发现优先使用 FastCtx MCP，机械替换支持预览与影响范围限制，`rg` 保留为后备方案。
- 同步阶段 B 交接、查询缓存与 SQLite 状态文档：高级筛选 F0–F3、轻量分析 F4a、功能结果设计 F4b-0 和 IPA 自动比较 F4b-1 已完成；下一步迁移 IPA 分布与正式形态分析，质量检查留给 F5，日期范围和来源数量查询不再被误列为待处理项。
- 词条详情操作区新增“定位当前词条”按钮，可在宽屏下展开已收起的词条列表、在窄屏下打开列表抽屉，并复用现有查询窗口定位与虚拟列表滚动将列表重新聚焦到当前词条；当前筛选不包含该词条时会保留筛选并给出提示。
- 完成高级筛选查询化 F3：标签、释义/例句/词条备注/来源/IPA 有无、多来源和创建/修改日期等稳定筛选不再保存完整词条 ID 数组，而是把结构化 filter descriptor 直接接入普通词条窗口、查询会话、远端定位、排序与刷新；查询型高级筛选内同时开放自由文本搜索，“当前搜索命中字段”复用相同搜索字段和 fuzzy 配置。IPA 生成、Gloss、形态与质量问题继续作为功能结果集，等待 F4b/F5 服务端会话。
- 词典管理列表不再显示词根数量；轻量应用状态只为当前词典计算词根数量，未加载词典只查询 metadata 和词条数，切换为当前词典时才建立其词根拓扑。稳定词根拓扑 LRU 由 8 本收紧为 2 本，保留当前词典与最近一本的来回切换能力。
- 为窄屏顶部应用栏的工具导航、词条列表和新建词条三个图标按钮补充应用内 tooltip，并继续复用按钮随界面语言更新的无障碍名称。
- 词根模式搜索中，因命中衍生词而自动展开的词根组会将单组展开按钮置灰，避免呈现可以收起但实际会被搜索逻辑立即重新展开的无效操作；仅词根自身命中的组仍可手动展开或收起。
- 收窄读取与整库写入契约：词条列表、词根组及组内衍生词端点固定返回摘要 DTO，无参数词条查询也使用默认窗口，不再提供 `include=full` 或完整词条数组旁路；完整词条继续由单词条端点按需读取。
- 删除无调用者的完整快照 PUT、整库 repository 写入包装、`queryEntrySummaries()` 和伪细粒度语料读取方法；JSON 导入/导出、当前词典快照 GET 及正式模块级保存边界保持不变。
- 移除与词条详情/词汇网络重复的来源文本和指定来源词条筛选契约；总览“衍生词”卡片进入词条列表时直接复用“有来源”筛选，不再额外生成和保存一份同结果的 `derivedEntryIds`。
- 收束后续查询规划：API 契约暂时移除尚未建模的语料块/单元读取端点预留；SQLite 计划记录数据分析词根家族应复用稳定后端拓扑，并在迁移后删除前端重复关系计算和未消费的家族成员 ID。

### 性能

- IPA 功能结果基础会话按词典 generation、source、引擎版本和 IPA 设置摘要复用，并受每词典 4 项、全局估算 32 MiB、2 分钟 idle TTL 与 LRU 限制；相同 descriptor 的并发构建会合并，生成循环分块让出事件循环。临时 SQLite 基准中，10k/30k 冷构建约 79/246 毫秒，热视图约 2 毫秒，暂不需要引入持久化任务队列或轮询状态。
- 用 filter-only `/entries/filter-facts` 取代 search-aware probe：批量 `EXISTS` 不再执行 strict/fuzzy 搜索，规范化重复条件在当前 generation 内复用；搜索结果可在无搜索时写入精确结构总数，在有命中时直接证明结构非空。
- 数据分析总览打开时只执行 planner 合并后的 `entryStats`、`partStats` 和 `activityStats` SQLite 聚合；页面未打开时不请求，已迁移卡片不再生成完整分析报告或读取完整词典 snapshot。写入后的 generation 与 cacheKey 会同步失效。
- 高级筛选日期条件由 `strftime()` 提取日期改为 UTC 日起点的半开区间比较，并补充 `created_at` 索引；来源数量上下界改为单个 `COUNT(*) BETWEEN`，不再为同一词条重复执行两次来源计数。分析页同步停止为已迁移的覆盖率、标签、词性、日期和当前搜索字段保留完整匹配 ID 集合。
- 无搜索的普通列表定位现在复用排名查询已经得到的结果总数，读取目标窗口时不再对同一词性、标签或其他结构条件重复执行一次 `COUNT(*)`；排名与窗口语义保持不变，也不新增完整结果物化或缓存。
- 词根模式搜索与普通词条搜索现在共享同一套静态/形态投射命中 SQL；词根模式只读取去重命中 ID，再映射到稳定词根拓扑，不再维护全词典候选 ID、经验分批和 JavaScript eligible-set 过滤路径，也不会为词根分组执行无用的词条排序。
- 词性与标签筛选改为由 `entry_tags` 候选 ID 集合驱动，不再以全部词条为外层逐条运行相关 `EXISTS`；新增 `(tag, entry_id)` 覆盖索引和 `position = 0` 的首标签部分索引，同时覆盖手动多词性、自动首标签词性及标签 `any/all`。10k 临时压力词典中，1416 条命中的同等词性查询由旧路径中位约 5.45 秒降至约 7.5–9 毫秒。
- 严格与 fuzzy 搜索统一复用后端有序 ID 查询会话：严格搜索不再分别重跑 `COUNT`、页面匹配和远端排名，翻窗直接切片缓存 ID，定位通过 `entryId → resultIndex` 读取目标窗口；无搜索的普通筛选继续使用轻量 SQL。10k 临时压力词典中，严格释义搜索冷/热约 33/8 毫秒，热会话远端定位约 3 毫秒。
- 搜索会话冷构建不再把结构筛选结果物化成 JS ID 数组后按 500 条分批或在超过 1000 条时全投射扫描并由 JS 求交：存在结构条件时改由物化候选关系直接连接静态/形态投射，无结构条件时直接扫描目标投射。10k/30k 形态压力词典中，约 14% 候选的严格与 fuzzy 搜索提升约 1.9–3.3 倍，未筛选搜索提升约 1.1–1.9 倍。

### 修复

- 桌面词条列表由 JS 同步视口坐标的 fixed 面板改为尺寸隔离的 sticky 槽位：横向位置和宽度随内容网格自然变化，内层可见高度不再撑大页面，滚到底部时保持稳定顶部间距且不产生额外空白；移动端继续使用原有抽屉。同步补齐 800px 响应式断点的亚像素缝隙，并关闭桌面根页面及移动端 workspace 的 overscroll，避免触摸板在滚动边界继续拖动页面。
- 修正词典管理页顶部中文副标题，将重复且生硬的“多词典管理”改为“多个词典”；英文文案与页面结构保持不变。
- 数据分析总览的资料覆盖行统一使用“释义 / 例句 / IPA / 备注”等字段名称，不再让前三类沿用“有……”的存在性筛选文案；点击后仍进入对应的“有 / 无”循环筛选。
- 修复词根、衍生词以及字段值组合型高级筛选在切换界面语言后仍保留旧标题的问题；系统标签由主 i18n 字典重新渲染，用户标签、词性和表格名保持原文，且不会触发查询刷新。
- 切换高级筛选变体时，只有在新变体实际返回首条候选后才消费自动选中状态；搜索暂时无命中时切换 IPA 结果分类、随后调整搜索恢复候选，不再让词条详情停留在上一分类。

### 安全与兼容性

- HTTP 请求体上限按端点职责拆分：普通单对象与设置保存为 5 MiB，模块级/批量保存为 32 MiB，JSON 导入为 64 MiB；合法 `Content-Length` 只用于提前拒绝，实际字节流仍会持续校验，超限错误同时返回对应的 `limitBytes`。

## 2026-07-18

### 改进

- 高级筛选查询化 F2 将结构化 `EntryFilter` 接入 `/entries` 及词条定位 API：释义、例句、词条备注、来源和 IPA 的有无，来源数量、创建/修改日期，以及按词条 ID 定位的衍生关系均由同一 SQLite 条件编译器执行；列表查询、窗口缓存与自动定位不再需要为这些条件维护不同语义。
- 高级筛选查询化 F1 新增共享、可序列化的 `EntryQuery/EntryFilter` 模型：筛选、自由文本搜索、排序、DTO 形状和分页窗口现在具有明确边界，现有 `/entries` 参数会先归一化为同一结构；查询会话 descriptor、cursor digest 和缓存键直接使用同一规范化身份，不再各自拼装平铺字段。
- 将 API、SQLite、查询缓存和高级筛选等技术文档整理到 `docs/`，新增文档索引并修正全部当前文档间的相对路径；根目录继续保留项目 README、Changelog、长期协作规则和阶段性交接入口。
- 全面复核 README、交接、API、SQLite、迁移与查询缓存文档的实现状态：将查询会话 Q1–Q4、词根搜索/定位、词汇关系 API、SWR 和局部词条渲染从待办改为已完成，修正当前 schema、错误码、JSON 定位和中等宽度导航说明，并保留高级筛选、分析/质量 API 与语料升级等真实待办。
- 完成高级筛选查询化 F0 清点：区分可直接进入 EntryQuery 的稳定条件、需要独立服务端结果会话的分析/质量结果，以及不属于筛选的导航动作；记录全部现有入口、刷新失效和语义债务，为后续移除前端完整 ID 数组与本地快照扫描确定边界。
- 正式移除旧 JSON runtime repository 与 `CONLEXICON_REPOSITORY` 后端切换入口，主服务固定使用 SQLite；旧 JSON 单文件导入、导出和目录迁移仍由独立 conversion/migration 边界处理，批量迁移脚本改为直接只读旧数据目录。
- 重整存储测试边界：删除 JSON repository 完整契约入口，将共享 repository contract 收紧为 SQLite 正式契约，并把模型与旧 JSON 转换检查拆为独立脚本；服务启动 smoke 现在只验证 SQLite 数据文件。
- 删除 repository contract 中已被逐值搜索 record 专项检查覆盖的旧标签扁平数组断言，避免重复绑定内部投射形状。
- 清理正式运行路径中已移除实体 ID 的代码残留：IPA 文本规则不再传递无效的 ID 预留参数或执行空的 scoped ID 校验，词条形态组和词典设置规范化也不再保留旧 ID options；旧 JSON 字段剥离仍集中由导入 migration 负责。
- “备注”搜索字段现同时覆盖词条备注、释义备注和词条形态组备注；不同形态组的备注仍按独立值匹配，词条列表的命中摘要会分别标明三种备注来源。该功能复用现有 SQLite 搜索投射，无需调整 schema 或加入旧字段兼容。
- 词汇网络第一版升级为分层 SVG 关系图：来源、当前焦点和直接衍生词按稳定节点 ID 布局，点击关联词条时复用已有节点并平滑重新聚焦、同步更新有向连线，不再执行固定 160ms 的整面板淡出重绘。关系未缓存时先保留可确定的相邻关系，响应返回后补入新节点；窄屏自动切换为纵向关系图，并补充节点计数、应用内悬浮详情、键盘激活、弹层焦点与 reduced-motion 支持。
- 新增并接通普通词条与词根模式的后端目标窗口定位：自动跳转会按当前查询直接装入包含目标的窗口，不再扫描完整活动词典推算页号；词根模式会先定位父级窗口，再读取整组衍生词，并支持指定多来源词条的目标根上下文。
- 窗口化查询正式区分顺序分页 `nextCursor` 与随机访问 `windowCursor`；普通词条和词根组响应在每个窗口都返回后者，前端不再借用可能在末页为空的“下一页”游标来加载远端窗口，为后端目标定位 API 提供稳定契约。
- 词条列表查询与按需详情读取接入两段式 stale-while-revalidate：有已完成内容时，读取开始后的 200ms 内保持原界面，超过后用统一的“正在更新”遮罩使旧列表或详情变淡；旧详情从请求开始即进入 `inert`，避免遮罩出现前或通过键盘继续操作旧词条。首次加载仍直接显示加载状态，请求失败则进入明确错误状态，不回滚或自动重试。滚动补载后续数据窗口继续使用局部占位，不触发整栏遮罩。
- 词根模式“全部展开”改为跨父级窗口的全局状态，不再要求全部窗口同时驻留；父级首窗提供轻量窗口统计以估算完整展开高度，组内内容仍按可见范围加载。全展开状态下可单独收起词根作为例外，父级窗口淘汰不会丢失该意图；全部展开或全部收起会清理上一轮例外。
- 查询 cursor 升级为绑定服务进程、词典缓存 generation 和查询 descriptor 的不透明版本化游标；非零远端窗口必须携带有效 cursor，写入、重启或查询条件变化会返回可区分原因的结构化失效错误，缓存淘汰则透明重算。
- 更新 handoff、SQLite 后端/迁移计划、API 契约和 README，移除搜索 S4、JSON 完整契约与早期目录拆分等过时规划，并记录查询缓存、数据窗口化、按需详情和关系拓扑已完成后的剩余查询消费者路线。
- 新增查询会话与结果缓存设计，明确前端紧凑查询缓存、后端运行时会话、查询身份、词典级失效、容量淘汰和可重建 cursor；缓存淘汰只触发重算，不影响分页正确性，并将列表 DTO、按需详情和词根组子项拆分提前到数据窗口化之前。

### 性能

- 词条详情的来源按钮、衍生词列表、完整编辑中的只读衍生词和词汇网络现在共用 `/entry-relations/:entryId` 及前端关系缓存；切换词条不再为每个来源和衍生词区域分别扫描、排序完整词典并重建关系索引。SQLite 来源响应同时返回匹配词条 summary，词汇网络也不再为了补全来源卡片遍历活动词典。
- 词条切换不再调用全局 `render()`：现在只更新已渲染卡片的选中态、按需词条详情、词汇网络可见状态和必要的列表高度/自动滚动，不再重建导航、筛选器、查询窗口、虚拟列表 DOM、卡片高度与 offsets。词汇网络关闭后跳转到当前网络节点也复用同一局部提交路径。
- 目标定位复用现有查询结构：严格词条查询在 SQLite 内计算排序位置，fuzzy 词条与词根组分别通过会话的反向下标直接定位；返回目标窗口时不需要前端加载或扫描完整活动词典。
- fuzzy 词条会话和词根组会话分别为已有有序结果建立 `entryId → resultIndex`、`rootId → resultIndex` 索引；索引随会话一次生成并受同一容量估算和 LRU 管理，为目标定位 API 提供 O(1) 的结果下标读取，不额外扫描 SQLite。
- 稳定词根拓扑新增 `entryId → rootIds` 和 `rootId → group` 反向索引，词根组子项读取不再线性扫描查询会话，词汇网络关系读取也不再为每次请求递归遍历祖先和后代；定位成本接近 O(1)，仅保留与实际返回组大小相关的 DTO 构建成本。
- 词根模式新增独立 relation generation 的稳定拓扑缓存：词根与全部衍生词的 ID 关系不再随搜索词、搜索字段、排序或无关保存重复构建；搜索查询直接在 SQLite 静态/形态投射中筛出命中 ID，再对缓存拓扑生成查询视图，不再导出完整词典 snapshot。词典摘要的词根计数也复用同一拓扑。只有词条增删、lemma/来源变化和整库替换会使拓扑失效；普通词条保存与批量 patch 仅更新轻量排序记录，其他模块保存完全保留拓扑。
- 查询缓存 Q4 将普通词条和父级词根组列表改为纯滚动数据窗口：分别按 200/100 的小窗口读取，各自最多保留 5 个已加载窗口；未加载页以完整结果总数建立等高占位，原生滚动条从首屏起即可代表完整列表，远端页按可见范围预取并按距离淘汰。单个词根组展开后一次读取整组衍生词，不再维护嵌套窗口、cursor 或单组硬上限。
- 窗口替换会继承占位页的高度比例，并在非滚动状态恢复现有虚拟列表锚点；自动跳转会先补载目标所在窗口，避免加载或淘汰远端页时让当前视口无故跳动。
- 查询缓存 Q3 将普通词条列表切换为直接消费 SQLite summary DTO，并以最多 12 项的按需详情缓存读取当前词条；列表不再把查询结果压成 ID 后回到完整词典快照取卡片数据，多义项预览与搜索命中位置仍保留。
- 词根模式的折叠组响应改为只返回词根 summary、衍生词数量和匹配数量；组内衍生词在展开后通过独立端点一次读取整组，避免折叠状态预载无关子项。
- 查询缓存 Q1 新增前端紧凑 LRU：词条查询、词根组和词性 facets 会复用成功结果并合并同 key 请求，缓存受 4 项与约 16 MiB 双重限制；保存、覆盖或删除词典后按词典失效，错误和过期代结果不会进入当前 UI。
- facets 查询键改用词典 `updatedAt`，不再为生成缓存签名扫描全部词条标签。
- 搜索输入防抖由 150ms 调整为 250ms，减少连续输入期间产生的中间查询，同时保持停止输入后的及时反馈。
- 查询缓存 Q2 新增后端运行时 `QuerySessionCache`：fuzzy 词条查询复用有序命中 ID，词根模式复用根/衍生关系 ID，页面内容仍从 SQLite 按需组装；相同查询合并构建，并受每词典 8 项、全局约 64 MiB、2 分钟 idle TTL 和 LRU 限制。
- 所有 SQLite 词典写入只在成功后递增运行时缓存 generation 并按词典失效；空 patch 和失败写入保留有效会话。查询命中路径同时改用轻量 metadata＋settings 上下文，不再读取无关 docs、corpus 或完整形态配置。
- 新增只读 Q2 查询会话基准脚本，可直接针对现有 SQLite 测试目录比较 fuzzy entries 与 root groups 的首次冷查询和重复热查询。

### 修复

- 统一移动应用栏、抽屉、词汇网络、应用内 tooltip、toast 和模态弹窗的全局 CSS 层级变量；toast 现在会显示在窄屏词条列表抽屉之上，同时仍低于需要用户处理的模态弹窗。
- 快速连续切换词条时，较早完成的详情请求不再覆盖后一次选择的自动滚动目标或关闭抽屉。
- 词条列表卡片的局部选中态同步补充 `aria-current`，切换词条和词汇网络返回后，视觉选中态与辅助技术读到的当前位置保持一致。
- 修复查询切换的 SWR 旧列表可能提前“完成”自动滚动、随后被新首窗替换而丢失目标的问题；待定位状态现在只允许由当前查询的就绪窗口完成。
- 为 Gloss 字体、字号、样式控件和逐字段搜索开关补充唯一表单字段名称，消除 Chromium/Edge 对缺少 `id` 或 `name` 的表单控件诊断，同时保持既有 `data-*` 保存逻辑不变。

## 2026-07-16

### 改进

- 搜索 S3.1 建立统一的逐值检索 record 契约：共享搜索模型现在为每个词形、标签、义项、备注和词源值保留字段及来源定位，现有 JavaScript matcher 也复用同一边界；SQLite 静态 projection 与 API `searchHits` 的后续结构和失效范围已同步写入契约文档。
- 搜索 S3.3 接通 SQLite 逐值 projection 与词条列表：静态非模糊搜索会返回实际命中的 `searchHits`，搜索摘要据此保留义项定位，不再为静态结果重复遍历本地字段或生成动态形态。

### 性能

- 搜索 S3.2 新增无独立 ID 的 SQLite `entry_search_values` 派生表；导入、词条保存、批量 patch、删除以及相关搜索规范化/标签替换设置变化会事务性维护词形、IPA、标签、释义、例句、备注和词源 records。动态形态不写入该表。
- 搜索 S3.3 将全部非模糊静态字段查询切换到 `entry_search_values`，覆盖 Unicode、NFC、case folding 和自定义等价规则，避免读取完整词典 snapshot。
- 搜索 S4 新增独立、无实体 ID 的 `entry_morphology_search_values` 派生表，保存形态求值顺序、真实模板组/子表/单元格坐标及原始/规范化值；词条写入局部维护，形态配置或搜索规范化变化全量重建，删除外键级联。严格及 fuzzy 的形态单字段、静态字段和混合查询现在均直接读取两张 projection，并返回统一 `searchHits`，不再导出完整词典或逐词条动态生成形态。SQLite 连接通过确定性函数复用共享 fuzzy 评分；大型候选集顺序扫描 projection，避免大型 `IN (...)` 查询和全部 records 的 Node.js 物化。
- 词条查询的结构筛选和无搜索路径不再为 summary 响应重建完整词条；前端查询失效键改用词典 `updatedAt`，搜索输入增加 150ms 防抖，避免每次按键都生成大型词典内容签名并立即发起读取。

### 修复

- SQLite 词条排序增加唯一 ID 作为最终稳定排序键，避免同词形、同时间及同位置记录在分页边界间漂移；普通词条列表和词根模式在 API 结果被 `hasMore` 截断时会在列表末尾显示最小加载数量提示。
- 补齐词条编辑、词条详情、筛选排序控件及各主要页面区域的无障碍名称本地化；切换中英文后，屏幕阅读器不再继续读出硬编码的中文标签。

## 2026-07-14

### 改进

- IPA 映射规则移除持久化实体 ID，改为按顺序保存的纯文本规则；词条形态组移除 `emorph` UUID，以 `entry_id + template_group_id` 作为组合身份和 override 外键。两类旧 ID 只在 JSON 导入迁移时删除，不再参与运行期跨类型 ID 防撞。
- 自由文本搜索现可按词典配置 NFC、Unicode 17 默认大小写折叠及自定义字面等价规则；规则按最长变体优先、单次替换，空规则和冲突规则会在保存前给出本地化提示。严格模式为默认值。
- 词条列表、词根模式、搜索摘要与高亮、搜索字段分析和词源自动补全共用同一搜索 normalizer；结构键与词源关系键继续保持各自既有的独立匹配边界。
- 标签筛选、手动词性识别、红色高亮标签和自动形态模板匹配改为大小写与码位精确匹配；SQLite `entry_tags` 不再保存 `normalized_tag` 派生列。词源 `source_key` 暂保持原有宽松匹配，之后改为稳定词条 ID 引用时再单独整理。
- 开发期 SQLite 移除没有实际升级脚本的 schema 版本字段与 migration 表；当前 schema 不匹配的测试库继续从 JSON 重建。下一个正式 release 才重新引入版本化 schema migration。
- 导入迁移现在会完成并清理旧词条、设置和语料字段；前端、核心模型与 SQLite 写入侧只按当前数据结构运行。前端启动也不再接受 `/api/state` 中的完整词典 payload，形态规则错误标签也不再识别旧单元格坐标格式。
- 其他设置的搜索配置改为按词形、IPA、标签、释义、例句、备注、词源和形态学分别保存“参与搜索 / 模糊匹配”开关；关闭字段搜索会禁用但保留其模糊匹配偏好，且不允许保存全部字段均关闭的配置。词源自动补全的模糊匹配也迁入同一 `search` 设置结构。
- 词条列表、词根模式、前端本地筛选、搜索命中摘要和数据分析的“当前搜索命中字段”现在统一读取词典级搜索字段配置；关闭形态搜索时不再生成形态搜索文本。读取 API 同步移除旧 `fuzzy` / `tagFuzzy` 参数，仅接受字段级 `fuzzyFields`。
- 基础自由文本搜索改为逐个独立值匹配：释义、标签、来源、备注和例句不再因彼此拼接而意外命中，字段级模糊匹配也遵守同一边界；需要多个标签或条件组合时应使用高级筛选。
- 词条列表搜索结果不再重复渲染首条释义；命中后续义项时保留命中摘要，并显示原义项序号（如“释义2”）。
- 精确搜索高亮现在会标出同一词形、释义、标签或命中摘要值中的全部非重叠命中；NFC、大小写折叠或自定义替换改变文本长度时，也会映射回正确的原始字符范围。

### 性能

- 词条读取 API 在搜索范围不含形态学、所有字段均为严格匹配、未启用大小写折叠或自定义规则且查询为可打印 ASCII 文本时，直接在 SQLite 中筛选静态字段；其他配置在 S3 检索投影完成前使用共享 JavaScript matcher。
- SQLite 的词典元数据、其他设置、IPA 设置、语言文档和语料保存不再在 repository 内部重建完整词典 snapshot，改为直接返回各自的局部 payload。
- SQLite 批量词条 patch 保存改为直接返回本次更新的词条及可选设置，不再为 repository 返回值重建完整词典 snapshot。
- SQLite metadata 读取改为轻量路径；autosave 现在拒绝缺少 docs/corpus 的无效请求，同一请求包含文档和语料时会正确合并两个局部结果。
- 词汇网络移除前端本地关系索引的双算与大型词典全量签名；改为直接等待 SQLite 关系 API，并使用词典更新时间作为轻量缓存键。
- 编辑表单现在按规范化后的语义内容判断是否有未保存更改：未改动的完整/局部词条编辑离开时不再触发保存确认；手动保存未改动的词条、词典配置、其他设置、IPA、形态学、语言文档或语料库时不再发送写入请求，并提示没有需要保存的更改。

### 修复

- 搜索规范化的“添加规则”按钮改用应用内统一的绿色勾线新增控件样式，不再引用没有配套样式的孤立类名。
- 修复新的搜索设置结构在词条列表卡片渲染中遗漏旧全局模糊变量，导致列表读取成功后仍显示“无法加载词条列表”的问题。
- 修复完整编辑在未改动、但当前设置不允许空发音、标签或释义时，会因 no-op 判断提前返回而绕过必填校验的问题。
- 词条编辑的必填校验统一改为应用内本地化 toast；一次保存会合并提示当前全部缺失的必填项目，不再让词形字段触发浏览器原生警告。
- 修复宽屏词条列表在新建、改名或局部保存后查询刷新时被暂时清空并回到列首的问题；刷新期间保留已渲染列表，查询完成后再定位待聚焦的当前词条。

## 2026-07-11

### 改进

- 其他设置的“显示”新增词条列表词性显示位置，可选择副标题、标签区或两者均显示；默认仅在副标题显示，避免重复渲染。
- 新建或保存无释义词条不再写入空释义对象；完整与局部编辑仍会提供仅存在于编辑态的空释义卡片。
- 词条详情中的形态表格组改为按子表实际宽度自动换列的响应式流式布局；窄屏仍保留单表横向滚动。
- 其他设置的“显示”新增词条栏目拖动排序，可统一调整详情与完整编辑中释义、词源、衍生词、形态学和词条备注的顺序；衍生词在完整编辑中仅只读显示。
- 完整词条编辑的栏目顺序调整为基本信息、释义、词源、形态学、词条备注，与详情展示及局部编辑的可编辑栏目顺序一致。
- 新增可写入显式临时目录的形态验收词典生成器与 SQLite 验收脚本，覆盖多表格组、子表、自动/手动模式、overlay、空单元格与空栏目设置。
- 形态表格编辑中的子表展开控件移至标题后方，改为带应用内 tooltip 的方向箭头；右侧仅保留删除操作。
- 其他设置的“显示”新增“空栏目”开关；默认不显示词条详情中的空释义、词源、形态学和备注区块，开启后保留它们作为局部编辑入口。
- 形态表格单元格留空时现在生成空白，不再默认引用词形；编辑器同时移除 `{}` 的灰色提示，显式填写 `{}` 仍表示引用词形。
- 形态表格配置页移除每个子表标题上方重复的“形态表格”眉题，保留可编辑的子表标题。
- 词条形态模型收束为 `morphologyMode: auto | manual`：SQLite `entries` 新增 `morphology_mode`，自动模式的词条级标题、备注和单元格例外按真实 `templateGroupId` / `tableId` 保存为 overlay；手动模式使用形态组顺序，空列表即明确不使用形态。自动转手动会实体化当前自动结果，并在其后保留 dormant overlay；编辑器以单一切换按钮在两种模式间转换，手动转自动会经确认放弃手动配置。
- 自动转手动同样改为应用内确认：弹窗说明会转移当前自动结果与隐藏 overlay，并提示之后恢复自动匹配会清空对应手动数据。
- 词条完整编辑与局部编辑的形态区改为按形态组和子表显示 override：支持 auto/manual 模式、自动命中组的词条级标题/备注/单元格 overlay、手动添加或移除多个模板组，以及手动组的上下调整顺序；旧单表下拉框不再参与保存。
- 旧 JSON 导入的形态迁移现在负责将旧 `entry.morphology`、`templateGroupId: "auto"` 和 `templateGroupId: "none"` 转换到当前 `morphologyMode` 形状；核心模型、repository 和前端持久化路径不再解释这些旧字段。开发期旧 SQLite 库不做 schema 升级，需从 JSON 重新导入。
- 新增 `lib/legacy-dictionary-migration.js`，集中处理旧 JSON 字段迁移、旧形态结构迁移和迁移报告；核心词典规范化模块改为只处理当前数据形状的默认值、ID 补齐和当前语义规范化，SQLite repository 不再内联 `morphology.tables` / `entry.morphology` 到新结构的迁移逻辑。
- 共享 `morphology-model` 正式切换为 `templateGroups`、组内子表、`sourceText` 单元格和词条 `morphologyGroups` / 嵌套 override 的当前结构；规则校验、动态生成和形态搜索均按多子表模型执行，模板组、子表和词条形态组 ID 纳入统一防撞检查。
- 前端完成形态学数据模型迁移的第一步：启动、词典状态规范化、ID 收集和词条保存改为保留 `templateGroups` / `morphologyGroups`；旧单表界面暂通过局部视图适配运行，避免共享模型升级后被误报为后端 API 断连。
- 词条详情页的形态展示已改为正式的模板组/子表渲染：一个词条可展示多个形态组及其全部子表，并按子表独立读取 override。兼容旧词典中“自动匹配 + override”的 `templateGroupId: "auto"` 记录，使其在匹配模板组的首个子表上正确生效。
- 自动形态学拆分为导航栏中的“形态函数”和“形态表格”两个独立页面；二者分别保存函数配置或表格配置，并在离开、保存快捷键中保持正确行为。旧 JSON 词典保存的 `toolNavOrder: "morphology"` 现在由导入迁移原位转换为两个新入口，前端不再长期兼容旧值。
- SQLite 读取与 JSON 导出停止回吐旧 `morphology.tables` 和 `entry.morphology` 字段；前端的临时单表编辑视图改由当前 `templateGroups` / `morphologyGroups` 在本地生成。
- 形态表格页面改为按模板组编辑：表格组可为空，可配置标题、备注和自动匹配标签，组内可新增、删除、排序多个独立配置尺寸和标题的子表；页面标题合并形态语法说明入口，新增操作采用绿色样式、删除操作采用红色样式。宽屏下备注紧随标题/标签右侧，并可向右扩展至最右上角的表格组删除按钮；窄宽度下备注下移而删除仍留在标题区域。子表标题后的重复尺寸摘要已移除，拖动排序限定由专用抓手发起。
- 形态表格配置页的普通单行输入按 Enter 不再意外提交保存；行数和列数输入按 Enter 会应用尺寸并重渲染表格。

### 性能

- SQLite `saveMorphology()` 在提交后只读取并返回形态模块，不再为模块保存路径重建完整词典 snapshot；API 响应维持既有 `{ id, updatedAt, morphology }` 契约。
- 词条形态组新增 `notes` 字段并写入 SQLite；详情页仅在对应形态组下展示该实例备注，模板组自身的备注不会泄露到词条详情或词条编辑界面。
- 词条详情中的子表标题不再重复写入表格左上角表头；标题仅显示在子表标题区域，左上角保留为空白的行标签表头。
- 词条详情的形态展示移除固定“形态组”前缀，仅显示实际配置的组标题和词条级备注。

### 修复

- 修复当前结构的 `morphologyMode: "auto"` 词条在 JSON 导入/转换为 SQLite 时，真实模板组 overlay（标题、备注、单元格覆盖）会被错误丢弃的问题。

## 2026-07-07

### 新增

- 新增 `scripts/check-default-repository.js`，通过临时数据目录真实启动 `server.js`，确认无环境变量时默认使用 SQLite repository，且 `CONLEXICON_REPOSITORY=json` 仍可显式切回 JSON repository。
- SQLite repository 骨架新增 `patchEntries()`，补齐批量词条 patch 语义，支持 `tags`、`pronunciation` 字段更新和同步 settings 合并。
- SQLite repository 骨架新增 `queryRootGroups()`，支持词根分组的搜索、排序、分页和 full/summary 返回语义；无搜索场景已使用 SQL projection 构建词根分组，搜索型请求仍保留完整共享语义回退。
- SQLite repository 骨架新增 `getEntryRelations()`，按现有响应格式返回来源匹配、衍生条目和同根组；来源和衍生查询已使用 SQL projection。
- SQLite repository 骨架新增 `getEntryFacets()`，支持词性 facets、标签频率和无词性计数的现有语义；当前已使用 SQL 聚合。
- SQLite repository 骨架的 `queryEntries()` 新增搜索、词性/标签/来源派生筛选、排序、分页和 summary/full 返回语义；结构化筛选、排序和分页已使用 SQL，下阶段再处理全文/模糊/动态形态搜索索引。
- SQLite repository 新增 metadata、settings、docs、corpus、morphology 和 IPA 模块级保存方法；当前已接入默认主服务，低频复杂模块仍通过 module blob 保存。
- SQLite repository 新增词条 CRUD，支持 `queryEntries()`、`getEntry()`、`saveEntry()` 和 `deleteEntry()`；当前已接入默认主服务，普通词条写入使用 SQL 增量 projection。
- SQLite repository 新增最小词典生命周期方法，覆盖创建、导入、导出、激活、删除、偏好保存和 state 读取；当前已作为默认 repository 使用。
- SQLite repository 骨架新增 JSON ↔ SQLite 最小无损往返：导入时写入完整词条 JSON、模块 blob 和核心查询 projection，导出时还原完整词典 JSON。
- 新增未接入主流程的 `SqliteDictionaryRepository` 骨架和 SQLite schema 初始化检查脚本，先验证 `.sqlite` 文件、schema migrations、核心词条表、模块 blob 表和第一批索引可在临时目录创建。
- 新增 `SQLITE_BACKEND_PLAN.md`，明确 SQLite 作为后续主存储的草案、schema 初稿、迁移顺序，并反推启动、词条查询、facets、词根关系、数据分析、质量检查、保存返回值和诊断修复等关键 API 契约。
- 新增 `SQLITE_MIGRATION_PLAN.md`，明确 SQLite 作为未来 canonical storage、JSON 退位为 legacy import/export profile，并规划转换服务、目录迁移脚本、导出 profile、备份策略和迁移报告。
- 新增 `dictionary-conversion-service` 骨架，集中 legacy JSON 导入解析和导出 profile 校验；`/api/import` 与 `/api/export` 已接入该服务，默认行为保持兼容完整 JSON。
- 新增 `scripts/migrate-json-data-to-sqlite.js` 迁移脚本，可将显式指定的旧 JSON data directory 转换到新的 SQLite data directory；脚本拒绝非空目标目录和嵌套源/目标目录，并输出 JSON migration report。
- 新增 `scripts/check-sqlite-migration.js`，使用临时目录验证 JSON 到 SQLite 迁移不会修改源目录，并能保留 active dictionary、界面偏好和词典内容。
- Repository 契约测试新增 corpus blob ID 防撞覆盖，确认未 SQL 化的语料 block/layer/unit ID 仍会与词条、释义等实体共享全局 ID 命名空间。
- 新增 repository/model 轻量检查脚本，使用临时数据目录验证词典规范化、导入解析、JSON repository 的创建、更新、导入覆盖、导出、删除、偏好保存和错误状态。
- 阶段 B2 开始接入词条级 API：新增词条列表、单条读取、单条保存和删除端点，并在 repository 检查脚本中覆盖这些端点。
- 新增设置、文档、语料、自动形态学、自动 IPA 与批量词条 patch 的模块级 API，为继续拆分完整词典保存路径做准备。
- 新增 `API_CONTRACT.md`，记录当前本地 HTTP API、完整快照兼容层、增量保存端点、实体 ID 校验范围和结构化错误码；`AGENTS.md` 增加同步维护 API 契约的长期规则。
- `API_CONTRACT.md` 新增阶段 B3 读取 API 草案，覆盖轻量启动状态、词条查询、facets、词源关系、语料读取和数据分析按需 summary 的后续契约方向。
- `API_CONTRACT.md` 补充共享查询索引、数据分析 widget query、planner、filter descriptor、诊断修复边界和 SQLite 兼容设计，作为后续数据分析 API 化与通用查询层抽取的草案。
- 新增第一批读取侧 API：词条列表查询参数、词条 facets 和词源/衍生/同根关系端点，并在 repository 检查脚本中覆盖。

### 改进

- 清理 SQLite 默认化后的文档状态：`NEXT_IMPLEMENTATION_HANDOFF.md` 移除过时的候选 SQLite schema 和“如果采用 SQLite”规划，`API_CONTRACT.md` 改为默认 SQLite 视角，`SQLITE_MIGRATION_PLAN.md` 明确 `index.json` 不承担 JSON/SQLite 存储分流。
- `SQLITE_BACKEND_PLAN.md` 按当前真实 SQLite schema 和默认 SQLite 状态更新，明确形态模板组/子表/词条形态组结构化表、`entries.etymology_description`、已移除 `entry_json`、后续实测事项和优化方向。
- `SQLITE_MIGRATION_PLAN.md` 更新 SQLite 默认切换清单和当前迁移策略，明确旧 JSON 词典暂时通过词典管理界面的 JSON 导入功能手动迁入 SQLite，自动迁移向导暂缓。
- SQLite 开发期 schema 新增 `entries.etymology_description` 投影列，并将形态学结构拆为模板组、子表、单元格、词条形态组和单元格 override 表；旧 `leftV/rightV` 设置暂时继续留在 morphology module blob，当前 SQL 开发中间态不做兼容迁移，测试 SQLite 目录需从 JSON 重新生成。
- SQLite repository 移除 `entries.entry_json` 存储字段；词条写入、单条读取、列表读取、词源关系、词根分组和导出快照现在都以 SQL projection 表为主结构，JSON 导出由 SQL 表组装生成。
- SQLite 检查脚本拆分职责：新增 schema/projection 检查、lifecycle smoke 和共享 SQLite 检查工具，`check-sqlite-repository.js` 改为薄聚合入口，完整行为一致性继续由 `check-sqlite-contract.js` 负责。
- `server.js` 默认 repository 切换为 SQLite；`CONLEXICON_REPOSITORY=json` 保留为 legacy/debug/回滚路径，README、API 契约和 SQLite 计划同步记录手动导入旧 JSON 词典的迁移方式。
- 前端启动流程改为先读取轻量 `/api/state`，再按需通过 `/api/dictionaries/:id` 加载当前词典完整快照；已加载且 `updatedAt` 未变化的词典会复用本地 snapshot，避免轻量 state 刷新时把前端词典内容覆盖为空。
- API 新增 `GET /api/dictionaries/:id` 完整词典快照读取端点，供启动、切换词典和兼容层按需加载使用；`API_CONTRACT.md` 同步记录轻量 state 与 active dictionary snapshot 的边界。
- 词典管理列表和删除确认的词条/词根统计优先使用 `summary.entryCount/rootCount`，避免轻量 state 下为 metadata-only 词典显示 0 或触发完整词典统计。
- 保存 API 响应继续收窄：metadata、settings、docs、corpus、morphology、IPA 设置、autosave 和批量词条 patch 现在返回局部 payload；前端保存后直接合并局部响应，不再为普通保存重新拉取 `/api/state` 和 active dictionary snapshot。
- 词条保存和删除的 repository/API 返回值继续收窄：`saveEntry()` 返回保存后的词条，`deleteEntry()` 只向 API 暴露更新时间；`patchEntries()` 暂时保持当前兼容路径。
- `SQLITE_BACKEND_PLAN.md` 更新形态学结构化 schema 状态，明确模板组、子表、单元格、词条形态组和 override 的 SQL 化边界，并记录函数/集合 DSL 源码不做函数级主表、生成结果和诊断只作派生缓存或索引。
- `AGENTS.md` 和 handoff 补充数据模型升级取向：只保证旧版本 JSON 能导入并转换成新格式，除非必要不为旧前端数据形状额外维护临时兼容层。
- `AGENTS.md`、SQLite 计划和迁移计划明确 JSON repository 冻结为 legacy/debug/迁移参考，不再要求随新增功能、schema 升级、读取优化或前端新数据模型同步更新；同时补充“任何对象 SQL 化前必须先判断必要性”的长期原则。
- `SQLITE_BACKEND_PLAN.md` 新增 SQLite repository 当前状态审计表，区分已 SQL 增量写入、已 SQL 查询下推、仍走完整快照/JS 全量逻辑和主服务尚未接入的部分。
- SQLite repository 的 metadata、settings、IPA、docs、morphology 和 corpus 模块保存改为 SQL 级写入，直接更新 `dictionary_meta` 或对应 `module_blobs`，并保留 IPA、形态和语料的局部实体 ID 校验。
- SQLite repository 的 `saveEntry()`、`deleteEntry()` 和 `patchEntries()` 改为真正 SQL 增量写入：只更新目标词条、释义 projection、标签 projection、来源 projection、相关 settings blob 和词典更新时间，不再为这些操作重写整库 projection。
- SQLite repository 的结构化读取开始下推到 SQL：无全文搜索的 `queryEntries()` 直接使用 `entries`、`entry_tags` 和 `entry_sources` projection 表完成词性、标签、来源、derivedFrom、排序和分页；`getEntryFacets()` 改为 SQL 聚合；`getEntryRelations()` 改为使用 SQL 来源索引查询来源和衍生条目。
- SQLite repository 的无搜索 `queryRootGroups()` 改为使用 SQL projection rows 构建词根分组、排序和分页，不再为普通词根模式入口导出完整词典；带全文、fuzzy 或动态形态搜索的 root groups 暂时继续回退共享 JS 语义。
- SQLite repository 的 `readState()` / `listDictionaries()` 改为只读取 `dictionary_meta` 并返回轻量词典 metadata 列表，附带即时 SQL 计算的 `summary.entryCount/rootCount`，避免启动索引阶段导出每个完整词典且不把派生统计写入 metadata；`API_CONTRACT.md` 同步记录正式接主服务前还需要前端按需加载 active dictionary。
- SQLite repository 的共享契约检查推进到完整契约，覆盖模块保存、批量词条 patch、偏好、导入覆盖、删除和错误状态。
- SQLite repository 的共享契约检查从 `entryCrud` 推进到 `readApi`，确认基础读取 API、facets、词汇关系、词根分组和读取一致性矩阵已与 JSON repository 当前语义对齐。
- Repository 契约测试 runner 新增早停阶段；SQLite repository 现在可复用同一套契约检查并通过到 `entryCrud` 阶段。
- Repository 检查脚本拆分为可复用契约测试 runner 和 JSON repository 入口；SQLite repository 已复用同一套 API/保存/导入导出语义检查。
- 建立最小共享 query 层，先接管数据分析中的词根/衍生关系统计和词根家族查询，为后续 API 化与共享索引查询打基础。
- 阶段 B 开始建立数据访问层边界：新增 `JsonDictionaryRepository`、词典模型规范化模块、API 路由模块、HTTP 工具模块和静态文件服务模块，将词典索引、词典 JSON 快照、导入、导出、创建、激活、删除、偏好设置、导入解析、数据规范化、API 分发和静态文件响应从 `server.js` 中抽离；`server.js` 现在只负责组装 repository、路由、静态服务并启动 HTTP 服务器，为后续索引、分页和 SQLite 评估保留兼容入口。
- 词典实体 ID 唯一性检查扩展覆盖释义、形态表和 IPA 映射规则；前端保存/导入提示与后端 repository 校验现在会一起识别这些对象的重复 ID，缺失 ID 的规范化补齐也会对这些对象和新词典 ID 执行静默防撞。
- 导入 JSON 时如遇词典 ID 格式无效，会提示是否作为新词典导入并重新生成 ID；未确认时仍拒绝无效 ID，避免静默改写来源数据。
- 新建、完整编辑、局部编辑和删除词条的前端保存路径开始使用词条级 API，完整词典快照端点暂时保留为兼容层。
- 其他设置、语言文档、语料库、自动形态学、自动 IPA、自动整理标签顺序和批量 IPA 生成改用模块级或批量词条 API；批量 IPA 生成检测到未保存 IPA 设置时会要求先保存设置再继续。
- 前端移除旧的完整词典保存 helper，普通运行期保存路径不再直接调用完整词典 PUT；后端完整快照端点仍保留为兼容层。
- 词典管理的名称、语言和描述保存改用词典元数据 API；页面卸载时的文档/语料自动保存兜底也统一走 autosave 入口。
- 自动整理标签顺序采用与批量 IPA 相同的严格逻辑：检测到其他设置未保存时，会要求先保存设置再继续应用排序。
- 批量词条 patch 增加字段白名单，目前仅允许更新 `tags` 和 `pronunciation`，并拒绝错误类型，避免批量端点绕过词条级校验修改嵌套结构或实体 ID。
- 增量保存改为局部实体 ID 校验：设置、文档、批量非 ID 词条更新等无关保存不再被历史重复 ID 阻断；词条、IPA、形态和语料等会改动实体 ID 的模块仍会检查本次保存范围内的冲突，导入和完整快照保存继续执行全量检查。
- 自动保存端点改为分发到文档和语料模块级保存，不再通过完整词典 patch 触发全量 ID 校验。
- 后端 API 错误改为返回稳定错误码；前端保存、导入、词典切换、偏好保存等失败时会读取错误码并显示本地化短提示，控制台仍保留原始技术错误。
- 词条列表普通模式新增读取 API 预备层：搜索、词性筛选和排序会调用后端词条查询 API，并在结果与本地筛选完全一致时使用 API 返回的顺序；不一致或请求失败时自动回退本地筛选。
- 词性筛选选项新增 facets API 接线：前端正常路径读取后端 facets，不再每次本地统计完整词性集合做一致性校验；本地统计逻辑暂时保留为 API 不可用时的轻量兜底。
- 读取 API 检查脚本新增一致性矩阵，覆盖普通搜索、标签显示替换、手动/默认词性识别、无词性、多词性、排序稳定性、分页和 facets，防止后端读取语义偏离前端旧行为。
- 词条读取 API 的词条排序和 facets 词性排序调整为与前端旧逻辑一致，减少读取预备层不必要回退。
- 形态学规则、表格规范化和动态形态形式生成抽出为前后端共享模块；词条读取 API 搜索现在会纳入自动生成的形态形式和词条级 override，并在一致性检查中覆盖。
- 词条读取 API 新增 `fields` 搜索字段参数，为后续手动配置搜索对象字段和例句升级为语料链接预留接口。
- 标签显示替换、词性识别、标签列表规范化、红色高亮标签和词条列表标签显示上限抽出为前后端共享模块，减少词条列表、搜索和 facets 之间的语义分叉。
- IPA 设置规范化、自动生成、规则上下文匹配、音节切分、重音解析和 IPA 对比工具抽出为前后端共享模块，为后续质量检查、数据分析和批量 IPA 逻辑后端化预留统一语义。
- 形态学规则引用解析、函数调用提取、替换语法校验和函数对象校验并入共享形态模块；后端保存形态配置时也会拒绝语法或函数对象错误。
- 词条搜索字段白名单、搜索字段文本抽取和读取 API 搜索文本生成抽出为前后端共享模块；前端本地筛选、搜索字段分析和后端词条查询现在复用同一套字段语义。
- 词条搜索的 fuzzy 评分与匹配逻辑并入共享搜索模块；读取 API 增加 `fuzzy`、`tagFuzzy` 和 `fuzzyFields`，为后续按字段分别配置模糊匹配预留接口。
- 质量检查的问题生成、Gloss 例句问题识别、词源网络问题识别、质量问题分组工具和词源来源解析抽出为共享质量模块，减少数据分析和高级筛选之间的语义分叉。
- 质量检查从数据分析子页迁移为独立工具导航页面；数据分析总览中的“质量问题”卡片现在跳转到质量检查页面，Glossed 例句分类只显示问题列表，不再额外统计 Glossed 例句数量。
- 词汇网络视图接入 `/entry-relations/:entryId` 读取 API；来源匹配和衍生关系优先使用后端结果，API 不可用时保留本地关系计算兜底。
- 词根模式新增 `/root-groups` 读取 API；前端词根模式正常路径改为直接使用后端词根组结果，不再每次本地计算完整分组做一致性校验，也不再在大词典 API 截断或失败时回退前端完整分组。API 返回分页截断结果时先渲染已返回页面，请求失败时显示失败状态，避免 3k/10k 测试词典把重型分组重新压回前端主线程。
- `NEXT_IMPLEMENTATION_HANDOFF.md` 和 `API_CONTRACT.md` 记录词根模式分页/窗口化 UI 暂缓到后端关系索引或 SQLite 评估之后再做，避免现阶段用简单无限滚动造成滚动条只能代表已加载页面的误导体验。
- 普通词条列表改为直接使用 `/entries` 查询 API 返回的顺序，不再每次先在前端本地筛选完整词条列表做一致性校验；本地筛选逻辑暂时保留给高级筛选、API 不可用和后续过渡路径。读取 API 临时上限提高到 10000，以避免未接入窗口化前截断当前 1k/10k 压测词典。
- 词性筛选选项改为直接使用 `/facets` 返回的词性集合，不再每次先在前端本地统计完整词性集合做一致性校验。
- 共享词条关系索引新增 `source key -> derived entries` 反向索引；词汇网络、词根模式、质量检查和 `/entries?derivedFrom=` 可复用同一关系索引，避免按来源查找衍生词时重复扫描全词条。
- 词根模式在 `/root-groups` 结果尚未完整加载时暂时禁用“展开全部”，避免大词典下该按钮重新触发前端完整词根分组。
- 数据分析总览移除“质量问题”卡片；数据分析渲染不再默认生成质量检查报告，质量检查仅由独立质量检查页面和质量高级筛选显式触发。
- 质量检查页面和质量高级筛选改用独立的质量报告构建入口，不再经过数据分析报告，也不再附带生成覆盖率、IPA、形态学、搜索字段和活动统计等无关分析内容。
- 词根模式、词汇网络和后端词源关系查询开始复用共享的词条关系模块；`API_CONTRACT.md` 补充后续词根模式读取 API 与关系索引优化设计。
- 数据分析页新增保守的报告缓存；词典内容、设置、语言、搜索或排序未变化时，页内切换标签会复用既有分析报告，避免重复全量重建。
- 质量检查页新增保守的报告缓存；词典内容、IPA 设置和界面语言未变化时，页内切换优先级/模块标签和刷新质量高级筛选会复用既有质量报告。
- 质量检查的词源来源解析和循环检测开始复用共享词条关系索引，减少来源检查时对全词条列表的重复扫描。
- 数据分析报告构建拆分为上下文、关系、覆盖率、标签、词形、IPA、形态学、搜索和活动等内部切片，并保留旧 report 输出结构；同时新增总览 widget 依赖声明，为后续可配置总览和按需分析 API 预留接口。
- 数据分析渲染开始按当前页面/子页构建必要切片；例如编辑进度页不再计算 IPA 或形态学统计，IPA 与形态学详情页也不再默认生成标签、词形和活动统计。
- 数据分析缓存从页面 report 缓存升级为切片缓存，不同页面和子页可复用相同的 IPA、覆盖率、关系、标签、词形、形态学、搜索或活动统计切片。
- 数据分析 relation slice 拆分为轻量关系摘要和完整词根家族排行；总览和覆盖率页不再为少量词根统计构建完整 `allRootFamilies`，只有进入“词根关系”子页或兼容全量分析时才生成完整排行。
- 数据分析 relation slice 与 rootFamilies slice 清理为互不覆盖的字段边界，relation 只提供关系计数和筛选 ID，rootFamilies 专门提供词根家族排行。
- 数据分析 rootFamilies slice 的摘要列表升级为未来可配置的 top N 词根家族，默认仍为 12，并在总览 widget 依赖声明中预留 `topRootFamilies`。
- 数据分析总览移除“标签频率”和“词根家族排行”卡片，改为显示覆盖率卡片，并同步收窄总览页请求的分析切片依赖。
- 标题栏词根数量改用共享关系模块的轻量计数逻辑，不再为显示统计文字而构建完整词根模式分组。
- 数据分析的页面/子页依赖、总览 widget 依赖、空切片结构、按需切片构建和切片缓存抽出为 `analysis-model`，前端仅保留依赖界面文案和高级筛选 action 的具体切片计算。

### 修复

- 修复词条虚拟列表高度估算过粗、动态高度测量可能改变滚动轨道，导致拖动滚动条时滚动条不跟随光标的问题；词条卡片估算改为按 body、标签 footer、质量问题行和搜索片段行分别计算，同一修复也覆盖语料虚拟列表的滚动回写。
- 有释义或搜索片段但没有 footer 的词条列表卡片改为视觉压缩样式；词条卡片副标题为空时不再渲染空行，高度估算同步纳入副标题有无。
- 虚拟列表的动态高度测量改为一帧内合并 offset 重建，减少快速拖动滚动条时由多次测量回调引发的主线程抖动。
- 虚拟列表在滚动活跃期间延迟应用动态高度测量结果，停止滚动后再统一修正 offsets，避免首次拖动大词典滚动条时持续扰动原生滑块映射。
- 虚拟列表滚动活跃期间将窗口渲染节流到约 50ms 一次，动态高度/offset 修正改为滚动中低频合并、停止滚动后约 180ms 带锚点补齐，减少大词典拖动滚动条时的主线程扰动。
- 虚拟列表新增运行时行高缓存，按宽度档、列表模式和行内容签名复用实测高度，减少大词典首次拖动后反复从粗估高度暖机造成的滚动轨道漂移。
- 修复数据分析总览移除“质量问题”卡片后，摘要指标网格仍按旧的六列布局保留右侧空位的问题。
- 修复局部编辑提交按钮误显示为“保存配置”的文案问题，改为通用“保存”。

## 2026-07-02

### 新增

- 中文品牌名更新为“构典”，并同步 README 与界面标题。
- 新词典的自动 IPA 设置默认开启“单音节词不标重音”。
- 新增 `Ctrl`/`Cmd` + `Enter` 新建词条快捷键；焦点位于输入框、文本框、下拉框或其他可编辑区域时不会触发，并沿用未保存编辑的“保存 / 放弃 / 取消”确认流程。
- README 新增快捷键说明，记录保存快捷键与新建词条快捷键。
- 词条详情中的未记载来源现在可点击创建为空来源词条，新建草稿会自动以该来源形式填充词形。
- 新增本地压力测试词典生成脚本，可在 `data/` 下生成使用规范类型前缀 UUID 的 1000 条随机词条测试词典。
- 其他设置新增“词条列表中显示原始标签”开关，可让词条列表标签忽略显示替换并直接显示原始标签。
- 其他设置新增词条列表标签显示上限，可配置 2–10 个标签 chip，默认为 3，并将省略号计入上限。

### 改进

- 新增 `AGENTS.md` 作为长期协作与工程约束文档，记录任务类型与授权边界、测试端口管理、验证命令、changelog 提交流程、README 更新要求、数据不变量、持久化边界和 UI 验收检查项；`NEXT_IMPLEMENTATION_HANDOFF.md` 改为引用该文件并专注阶段性交接；README 补充响应式应用外壳说明。
- 逐步淘汰原生 title 提示：导航与 Gloss 样式等应用内 tooltip 控件改用 `aria-label` 翻译键，并移除运行时清理导航 title 的旧逻辑。
- 删除旧的 `data-i18n-title` 本地化兼容逻辑，后续提示文案统一通过 `aria-label` 与应用内 tooltip 承载。
- 应用启动时使用统一的 `app-booting` 水合阶段隐藏主界面壳，等待后端状态、语言、响应式导航和首轮渲染完成后再显示，减少刷新时默认标题、按钮、控制栏和导航状态短暂闪烁。
- 启动阶段会读取本地主题缓存以提前应用暗黑背景；真实主题设置仍以 `data/index.json` 为准，并在后端状态返回或主题切换后同步缓存。
- 释义编辑中的“例句”和“释义备注”改为按需添加：默认只显示释义输入框，有既有内容时自动展开，否则通过“添加例句”和“添加释义备注”按钮手动创建；完整编辑和局部编辑保持一致。
- “添加释义”“添加例句”和“添加释义备注”统一使用绿色描边的正向添加按钮样式。
- 词典管理中的 JSON 导出入口移至词典列表卡片，每个词典都可直接导出；当前词典在列表中以绿色“当前”状态按钮标记，切换当前词典的入口也统一保留在列表卡片内。
- 移动端新增紧凑应用栏，并将工具导航与词条列表接入抽屉式打开/关闭骨架；选择模块、选择词条或新建词条成功后会自动回到主内容区。
- 触发标签、词性、高级筛选或数据分析“当前搜索命中”等词条结果入口时，会自动显示词条列表：移动端打开词条列表抽屉，桌面端自动展开已收起的词条列表。

### 修复

- 修复移动端抽屉打开时，保存/放弃/取消等通用确认弹窗会被抽屉遮挡的问题；确认弹窗层级现在高于工具导航与词条列表抽屉。
- 修复词条列表隐藏或抽屉关闭时触发词条跳转/保存后，自动滚动可能因隐藏状态测量不可靠而失效的问题；现在会记录待聚焦词条，并在列表重新显示后滚动到当前词条。
- 修复极窄宽度下词条详情头部操作按钮换行后仍被左侧标题与标签块撑开，导致按钮与标签之间出现大块空白的问题。
- 修复极窄移动端下内容重排可能挤到顶部应用栏上方或被顶部应用栏覆盖的问题；移动端应用栏改为占位的 sticky 首行。
- 修复中等宽度下刷新时工具导航先按展开状态渲染再自动收起、词条列表跟随展开态位置短暂移动的问题；启动/稳定阶段会暂时禁用关键布局过渡，并在显示前先完成词条列表测量。
- 修复刷新或开屏时词条列表在 fixed 布局变量尚未测量完成前短暂出现在左上角的问题；词条列表只有在布局测量 ready 后才进入 fixed 定位。
- 修复新建词条保存后、完整编辑或局部编辑改名导致排序位置变化后，词条列表不会自动滚动到保存后词条位置的问题。

## 2026-07-01

### 新增

- 质量问题高级筛选会在词条列表卡片下方显示对应的问题类型小卡片，并在悬浮时展示包含优先度、模块和具体问题的详情卡。
- 词条列表高级筛选条新增刷新按钮，并将循环切换按钮独立为刷新按钮右侧的单独控件。
- 词条列表的基础词性筛选新增“无词性”选项，可筛出未识别到词性的词条。
- 质量检查新增 IPA 问题类别，纳入缺少 IPA 与多个主重音检查；多个主重音按中优先级处理，并参与模块循环筛选。
- 词条列表筛选/排序行新增常驻的新建词条图标按钮，详情页滚动到任意位置时仍可快速新建词条，并使用应用内 tooltip 说明功能。
- 全局黑/白主题选择现在会保存到 `data/index.json`，重启或刷新后会恢复上次选择。

### 改进

- 词条列表高级筛选控制统一为单行控制条，长筛选名称会优先截断以保留刷新、循环和退出按钮。
- 数据分析质量检查页将类别切换与高级筛选入口整合：类别按钮保留数量与空状态变灰，只切换本页问题列表；“在词条列表查看”按钮负责进入高级筛选。
- 数据分析中的词性分布，以及标签频率中被当前词典识别为词性的标签，现在会触发词条列表的基础词性筛选，而不是标签高级筛选。
- 词条列表卡片右键不再直接新建衍生条目，而是打开可扩展的词条操作菜单，提供编辑、新建衍生条目和删除条目等操作。
- 词典管理入口移入左侧工具导航，默认排在最下方，并纳入按词典保存的工具导航重排设置。
- 标签排序、质量检查高级筛选、自动形态学语法和 IPA 音节划分说明统一改用同一套信息弹窗，并将说明按钮提示统一为应用内 tooltip。
- 局部编辑模式下的词源来源输入框现在也支持来源词条自动补全，与完整编辑页共用候选、键盘选择和失焦隐藏逻辑。
- 将高级筛选图标按钮、词根展开/收起、IPA 规则和语料库图标按钮、省略标签 chip 的原生 title 提示替换为应用内 tooltip；IPA 虚拟键盘移除重复符号提示，仅保留无障碍标签。
- 省略标签 chip 的应用内 tooltip 现在会按词性标签和红色高亮标签的样式渲染隐藏标签。
- 词条列表中可见标签 chip 的应用内 tooltip 改为统一显示“原始标签”和已配置的“显示替换”，避免依赖不稳定的视觉截断检测；省略号 chip 仍只显示被隐藏标签列表。
- 其他设置中的 Gloss 样式控件改用应用内 tooltip，不再触发浏览器原生 title 提示。

### 修复

- 修复高级筛选刷新后当前筛选已无匹配对象时仍残留旧词条的问题；若其他可循环变体仍有结果，则继续显示循环按钮。
- 修复无标签或无释义词条在词条列表卡片中标签与质量问题小卡片位置、尺寸异常。
- 修复缺少释义/搜索片段的词条卡片仍保留普通卡片最小高度，导致标签或质量问题标签上方出现大块空白的问题。
- 修复质量问题高级筛选在切换界面语言后，当前筛选名称和词条卡片问题标签仍停留在旧语言的问题。
- 修复标签组合、词性、词长、IPA 音位等统计型高级筛选名称在切换界面语言后未完整更新的问题。
- 修复 IPA 自动生成一致、宽松不一致、严格不一致等循环高级筛选名称在切换界面语言后未更新的问题。
- 修复词条列表高级筛选控制条中标签、图标按钮和退出按钮在中英文模式下实际高度与垂直对齐不一致的问题。
- 修复词条列表高级筛选标签过长被省略时无法查看完整内容的问题；溢出标签现在会使用应用内 tooltip 显示完整名称。
- 修复质量问题高级筛选中词条卡片位于列表底部时，质量问题悬浮详情向下展开被截断的问题；下方空间不足时会自动改为向上展开。
- 修复向上展开的质量问题悬浮详情在鼠标移开淡出时短暂跳回下方的问题。
- 修复质量问题悬浮详情在列表底部附近触发时可能扩大滚动区域、导致滚动条闪烁且详情卡无法稳定显示的问题。
- 修复跳转到词条时，虚拟列表平滑滚动可能被卡片实测高度更新打断，导致词条列表自动滚动偶发未到位的问题。
- 修复词源来源词条自动补全可能被旧的失焦延时隐藏任务误隐藏，导致提示框偶发不显示的问题。
- 修复词条详情页向下滚动、标题离开视口后，词条列表仍按标题占位计算高度而在下方留下额外空白的问题；桌面展开布局下词条列表会作为固定侧栏按当前视口可用高度延长，不再参与页面自然高度计算。
- 修复桌面工具导航展开/收起过渡期间，固定定位的词条列表可能滞留在旧横向位置，并在下一次切换时显示到相反错误位置的问题。
- 修复右侧详情滚动触发词条列表重测时，虚拟列表因反复清空实测行高而逐渐漂移的问题；非底部状态保持顶部锚点，已在底部时保持底部锚点。
- 修复关闭词汇网络后虽然跳转到网络当前词条，但词条列表不会自动滚动到对应卡片的问题。
- 修复宽屏英文界面下 Conlexicon 标题与词典管理、导航收起按钮重叠的问题；词典管理入口移入工具导航后，英文品牌标题不再进行额外响应式缩小。
- 修复应用内 tooltip 在快速移出按钮、窗口失焦或导航布局切换时偶发滞留不消失的问题。
- 修复暗黑模式下浏览器原生滚动条可能仍按浅色方案渲染的问题。
- 修复浅色模式下词条列表省略标签 chip 的应用内 tooltip 仍使用深色背景、与标签展开场景不协调的问题。
- 修复词条列表标签显示上限和 IPA 默认重音输入可能触发浏览器原生未本地化校验提示的问题；相关错误改由应用内本地化提示处理。

## 2026-06-30

### 新增

- 其他设置新增词性标签配置；可手动指定哪些词条标签识别为词性，并支持同一词条在词条列表、详情、筛选和数据分析中显示与统计多个词性。
- 其他设置新增自动整理标签顺序，可按统一标签列表批量重排词条标签并在执行前弹窗确认。
- 数据分析的 IPA 自动生成检查新增“一致”统计项，并可与宽松不一致、严格不一致高级筛选互相切换。
- 数据分析覆盖率条目的高级筛选入口统一为可循环筛选；有释义、IPA、形态表格与总览入口保持一致，有例句、有备注、有来源可切换到对应的“无”筛选。
- 数据分析质量检查页的选项卡和高级筛选入口按优先度、检查模块分组；模块筛选覆盖词形、标签、词源网络、Glossed 例句和其他问题，并支持循环切换。

### 改进

- 词典管理、其他设置、自动 IPA、自动形态学、语言文档和语料库的未保存离开确认改为“保存 / 放弃更改 / 取消”三选项；语言文档和语料库在自动保存关闭时适用。
- 高级筛选循环切换按钮的箭头图标改为居中显示。

## 2026-06-24

### 新增

- 新增词典级语料库，支持有序语料块、发言人/模态语料层、独立语料单元、父级链接、属性继承、标签与备注。
- 新增语料完整性检查，检测缺失单元、重复父级与实体 ID 冲突。
- 新增语料单元 Gloss 名称渲染，并可分别配置单元卡片、单元内容标题和词条例句的渲染对象与对齐方式。
- 新增 `\gla`、`\glb`、`\glc`、`\ft` 的独立字体、字号、粗体、斜体与 small caps 样式设置。
- 新增语料库与语言文档自动保存设置，并在关闭页面或程序时执行最终保存。
- 新增词典导入 ID 冲突确认，可选择覆盖现有词典或取消导入。
- 新增全局界面语言记忆，保存在 `data/index.json` 中。
- 新增词条列表和词条详情标签点击筛选；主标签使用普通词性筛选，次级标签使用高级筛选。
- 新增词典级左侧工具导航排序。
- 自动 IPA 映射规则支持拖动卡片调整顺序，并按词典保存规则优先级。
- 桌面工具导航支持在完整导航与图标栏之间切换；宽屏默认展开并在当前会话记忆选择，中等宽度强制使用图标栏且隐藏切换按钮。图标栏提供稳定的中英文悬浮/焦点提示、无障碍状态和更直观的功能图标。
- 桌面词条列表支持完全收起与重新展开，纯图标按钮置于词典标题之后，使用应用内提示以及展开向下、收起向右的箭头；主内容区会自动扩宽，搜索、排序和虚拟列表状态在切换后保持不变，并修复 800–980px 双栏布局下列表高度过早受限的问题。

### 改进

- 页面切换时的局部编辑和完整编辑行为改为“保存”“放弃更改”或“弹窗提示”三种模式。
- 词根模式跳转到未展开的衍生词时会自动展开目标词根，并正确定位多来源词条的对应实例。
- 语料单元编辑内容现在可以即时更新渲染预览，不必等待保存完成。
- 自动保存不再退出当前语料编辑状态，也不会清除当前草稿和撤销历史。
- 数据分析与其他设置卡片改为响应式瀑布流布局，长卡片不再撑开同一网格行。
- 自动 IPA、自动形态学和其他设置移除不必要的最外层大卡片，使子区域边界更清晰。
- 其他设置改进宽屏空间利用，并调整渲染对象、标签替换等长文本输入区域。
- README 补充语料库、Gloss 样式、保存策略、导入保护与全局语言设置说明。
- IPA 映射改为严格按照列表从上到下匹配；规则始终读取原始词形，生成结果不会被后续规则再次读取，较早规则消耗的字符不会再参与后续匹配。
- 改进中间宽度与窄屏下的通用布局收缩和换行；词条详情、设置、分析等卡片不再被内部 Grid、Flex 或长文本撑出边界。
- 形态表格与对齐 Gloss 在空间不足时保持卡片内部局部横向滚动，不再引起页面级横向滚动。

### 性能

- 全局渲染改为只更新当前打开的功能模块；未打开数据分析时不再生成分析报告。
- 词条列表、词根模式衍生词和语料列表加入动态高度虚拟滚动，页面只保留可见区域附近的卡片。
- 虚拟列表保留选中项跳转、词根展开、多来源上下文与窄屏独立滚动行为。

### 修复

- 修复语料库为空时仍存在额外滚动空间的问题。
- 修复 Gloss 多行对齐未生效、`\ft` 参与对齐以及对齐后字号异常缩小的问题。
- 修复语料自动保存导致编辑状态、草稿和撤销记录丢失的问题。
- 修复词根模式下跳转到衍生词时可能定位到错误实例的问题。
- 修复设置和分析网格因超长卡片产生大面积垂直空白的问题。
- 修复原生单行输入框裁切 `d͡ʒ`、`t͡ʃ` 等跨字符组合附加符号的问题；IPA 映射、音节规则、沙盒输入以及完整和局部词条发音现在使用保持单行行为的文本区域。
- 修复特定窗口比例下词条详情子卡片和操作区超出父卡片，以及最窄视口仍出现页面横向滚动的问题。

### 安全与兼容性

- 词条、语料块、语料层和语料单元使用跨类型实体 ID 唯一性检查，前后端保存时都会阻止重复 ID。
- 保持旧词典 JSON 的配置默认值与数据结构迁移逻辑，新增设置缺失时会使用兼容默认值。
- `data/` 继续保持 Git 忽略，个人词典不会进入版本控制。
